# 提交消息（按测试题 §07 格式）

> 直接复制下面代码块的内容作为提交消息。

```
姓名：wys　　实际投入时间：约 6 小时
演示地址：https://oi1784105-spec.github.io/ai-intel-station/
源码地址／文件：https://github.com/oi1784105-spec/ai-intel-station
未完成或未验证事项：
  1. 定时触发：**已由 schedule 事件自然触发过一次并成功** —— GitHub run 35165566489，
     event=schedule、11 步全绿、Pages 部署成功（UTC 2026-09-17T00:12）。该次比配置时刻
     （UTC 22:00 = 北京 06:00）晚约 2 小时，属 GitHub 调度排队。仍未观察到的是「连续多日准点触发」，
     交付期内无法验证，如实标注。
  2. 有意未做（非遗漏）：
     · AI 摘要生成 —— 砍单顺序第 1 位，避免引入外部 API 依赖与密钥；
     · 抓取文章页 og:image —— 需要 440 次额外请求，且带来图片体积 / 防盗链 / 反爬三类问题；
     · 源级超时预算与熔断 —— 当前只有 Google AI Blog 一个源受影响；
     · 中文纯标题的跨源事件聚合 —— 宁可漏聚，也不虚报「多来源报道」。
  3. 已知限制：
     · 配图率 26.4%（440 条中 116 条）是「不额外请求文章页」约束下的天花板；
     · 近 7 天运行成功率 0.73（口径：整轮无失败源的轮次占比，19/26；按源次计则为 409/416 = 98.3%）——
       唯一的失败源是 Google AI Blog。实测根因不是墙也不是代理：blog.google 的 4 个可用地址里
       有一半不可用（其中一个 TCP 握手通过、TLS 卡死），该源成败取决于当轮解析到哪个地址，
       失败被如实记录，且不影响其它 15 个源与已入库数据；
     · 抓取全部失败时 data/meta.json 会被写成空（已知缺陷，未修；news.json 不受影响）。
  4. 交付前自检：已把测试题 §03（8 个模块）/ §05（提交清单）/ §06（验收会检查的六项）当成
     验收单逐条走了一遍 —— 含「按 README 在干净环境跑通」「固定输入重复导入」「单个来源不可用」
     「缺少日期与长标题」「搜索无结果」「随机抽查原文」。逐条结论与证据见 VERIFICATION.md §1.10；
     其中「缺少日期」按测试题允许的方式，用明确标注的本地测试数据演示（线上 440 条发布时间齐全）。
     自检还抓出四处不达标并已修：① 源站 feed 没给摘要的条目只剩一个标题（补「来源简介」兜底）；
     ② 缺时间文案「时间未知」→「发布时间未知」；③ .gitignore 补上 .env 防线；
     ④ 换一批数据后，窄屏轮播里「无图 + 无摘要」的重点条目只填 46% 高。第 ④ 项先补过「源色
     占位块」，试用后按反馈撤掉 —— 那块占位在窄屏更像在「报警」（写着本条未提供配图），而没配图
     本来就该只显示文字：于是改为撤掉占位块与全部「无配图」提示，余量交给摘要 / 来源简介那一行
     撑开并在行内垂直居中，各屏回到 89–96%，另补两条断言钉死「窄屏不出现『无配图』字样」。
     逐条证据见 VERIFICATION.md §1.10。
  —— 数字口径：以上为二稿上线时的数据目录快照（440 条 / 26 轮；首次准备提交时为 434 条 / 25 轮）；站点每日自动更新，条目数会随之增长，
     页面内联读数与 VERIFICATION.md §1.7 的字节数 / 哈希取自同一批数据。
```

---

## 各项依据（供核对，不必提交）

| 字段 | 值 | 依据 |
| --- | --- | --- |
| 实际投入时间 | **约 6 小时** | 今日一次性完成：源站侦察 → 采集管线 → 前端与单文件 → 测试与离线样本 → 云端部署上线 → 体验改造。问题发现与解决过程记录在 `DEVLOG.md` |
| 演示地址 | <https://oi1784105-spec.github.io/ai-intel-station/> | 线上实测 HTTP 200、与本地 `dist/index.html` **字节级完全一致**（已是第六版：窄屏横向轮播 + 来源简介兜底；没配图的重点条目在窄屏走纯文字、不显示任何「无配图」提示；确切字节数与哈希见 `VERIFICATION.md` §1.7） |
| 源码地址 | <https://github.com/oi1784105-spec/ai-intel-station> | 含 `README.md`、`DEVLOG.md`、`VERIFICATION.md`、`docs/PLAN.md` 与全部源码、测试、离线样本 |
| 未完成或未验证 | 见上 | 逐条对应 `VERIFICATION.md` 第 2、3 节 |

## 自选亮点（测试题 §04：发现了什么问题 / 为什么选它 / 少做了什么、多获得什么）

**亮点：阅读账本** —— 一个落在「阅读效率」上、只用本机存储实现的自我记忆。

- **发现了什么问题**：站点每天自动更新、条目会累积（当前 440 条），但页面不记得
  **「你已经读过哪些」**。第二天打开，新条目和昨天看过的混在一起，要靠回忆或翻找确认
  「上一条读到哪儿」，也没法一眼看出「今天有没有新东西」。
- **为什么选它**：它作用在阅读效率与信息组织上，而且改动小 —— 浏览器本地
  `localStorage` + 顶部一行统计 + 两个筛选按钮，**不引依赖、不发请求、不动抓取管线**，
  所以不会因为源站接口变动而失效，也不必为它加任何密钥或后端。
- **用户少做了什么**：不用再记住「读到哪儿」。点开条目即视为已读（该卡片压暗到 55%，
  **不隐藏、仍可回看**）；顶部账本行常显「自上次来访 · **新增 N 篇未读** · 全站未读 M 条 ·
  已读 R 条 · 收藏 B 条（只记录在本机浏览器）」，配一条已读进度条；点「只看未读」即滤掉读过的，
  一键「全部标为已读」清空进度。已读标记是**就地改一个类、不整表重绘**，所以滚动位置与焦点不会跳。
- **多获得了什么**：每天打开有明确的「收尾感」—— 新增多少、还剩多少未读、已读进度一眼可见；
  状态只写在本机浏览器（`localStorage`：`ai-intel-read` / `ai-intel-last-visit`），刷新与关掉重开都在，
  不上传任何数据。账本行本身也是**功能可发现**的入口：藏起来就等于「只看未读 / 已读变淡」没人知道。
- **可操作成果（可现场演示）**：打开站点 → 顶部显示「首次访问 · 全部 440 条均为未读」→ 点开一条 →
  卡片变暗、已读计数 +1 → 点「只看未读」→ 它从列表消失 → **刷新页面** → 已读状态与进度仍在。
  这一条有自动化断言守着：`tools/verify_ui.py` 用同一个浏览器 profile 连开两次页面，验
  「已读状态在刷新后依然保留」「新增条数与入库时间口径一致」「阅读账本没被窄屏藏掉」（详见 `VERIFICATION.md`）。

> 其它发现与解决（跨源聚合、窄屏无配图只显示文字、未读对比度、右栏空洞等）不算自选亮点，
> 已如实记录在 `DEVLOG.md` / `README.md` 的「问题 → 解决」表里；测试题 §04 明示「不按数量加分」，此处只讲一个。

## 交付材料对照（测试题 §05 提交清单）

| 测试题要求 | 对应文件 |
| --- | --- |
| ① 可访问的网站 | <https://oi1784105-spec.github.io/ai-intel-station/>（即 `dist/index.html`，每日自动更新） |
| ② 完整源码 | 仓库根目录；依赖 `requirements.txt` / `requirements-dev.txt`；更新任务配置 `.github/workflows/`；**不含任何密钥**（本项目零配置、零密钥，故无需环境变量示例） |
| ③ 简短 README | `README.md` —— 本地运行、部署、技术选型理由、数据来源、更新方式、已知限制、成本与外部依赖 |
| ④ 开发说明 | `DEVLOG.md` —— 实际投入时间、主要开发工具与 AI 使用情况、两个关键决策、**问题发现与解决过程（主线）** |
| ⑤ 验证记录 | `VERIFICATION.md` —— 真实数据获取、固定输入重复导入、单个来源失败、任务配置与运行记录，并明确列出「已验证 / 未验证 / 未完成」；**另加 §1.10「按测试题逐条自检」**（含干净环境跑通、缺少日期 / 长标题 / 无图 / 搜索无结果、抽查原文） |
| ⑥ 打包 html | `dist/index.html`（单文件，数据已内联，双击即可打开，需另行发送） |