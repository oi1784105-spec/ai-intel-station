# DEVELOPMENT —— 开发过程、问题发现与解决记录

> 按测试题要求如实记录开发过程与投入时间。**本文主线是「怎么发现问题、怎么解决」**。
>
> **实际投入：约 6 小时**（同日一次性完成：源站侦察 → 采集管线 → 前端与单文件 → 测试与离线样本
> → 云端部署上线 → 体验改造）。
>
> 本文不记录逐段钟点。文中出现的时刻只有一类 —— **源站数据里的时间戳**，
> 那是排查时区缺陷的证据，不是工作时长。每个结论都对应仓库内可复现的命令或已提交的产物。

## 一、投入与产出总览

| 项 | 内容 |
| --- | --- |
| 实际投入 | **约 6 小时** |
| 完成范围 | 16 个实测可用源的采集管线（解析 / 归一化 / 四段去重 / 事件聚合 / 热度排序）、单文件静态站、205 项离线测试、离线逐字节重放、GitHub Actions 每日自动更新、GitHub Pages 线上部署 |
| 发现并修掉的问题 | **16 项**：起步阶段 6 类（第四节）、其他返工 6 项（第三节）、首版之后 7 处（8.3） |
| 交付物 | 线上站点 + `dist/index.html` 单文件 + 完整源码 + `README` / `DEVLOG` / `VERIFICATION` |

**一句话总结**：这 6 小时里最有价值的产出不是代码，而是**三类发现问题的机制** ——
（1）**实测探针**在写配置之前就把源缺陷挖出来；（2）**机械校验**（测试 + 逐字节比对）替我抓住
自己看不出来的错；（3）**真的去看页面**抓住静态断言永远抓不到的问题。见第三节开头。

## 二、阶段与产物（按阶段顺序，不记录钟点）

| 阶段 | 做了什么 | 产物 |
| --- | --- | --- |
| 阶段 1 · 源站侦察 | 可达性 + 字段质量 + 时间可信度逐源实测；剔除 7 个失效源 | `tools/audit_sources.py`、16 源清单确定 |
| 阶段 2 · 采集管线 | 建仓 + 全部管线模块 + CLI | `pipeline/*.py`、`collect.py` |
| 阶段 3 · 首次全量真实采集 | 真实网络跑通整条链路 | `data/news.json` |
| 阶段 4 · 返工期 | 6 项真实缺陷定位与修复（第三节） | 见第三节 |
| 阶段 5 · 前端与单文件 | 原生前端 + 数据内联打包 + 自包含校验 | `web/index.html`、`dist/index.html` |
| 阶段 6 · 测试与离线样本 | 205 项测试、17 个样本、逐字节重放 | `tests/` |
| 阶段 7 · 部署与上传 | 两个 workflow、建仓、推送、Pages 上线 | `.github/workflows/`、线上站点 |
| 阶段 8 · 体验改造 | 四项改造 + 新增前端点击校验（第八节） | `tools/verify_ui.py` |

## 三、问题发现与解决记录（本文主线）

### 3.0 三类发现手段 —— 这一节最值得记的东西

16 个问题里，**没有一个是靠「仔细读代码」发现的**。它们分别来自三条通道：

| 通道 | 抓到了几个 | 为什么有效 | 典型例子 |
| --- | --- | --- | --- |
| **① 实测探针**（写配置/写代码之前先打源站） | 6 类 | 源站的脏数据是「外部事实」，猜不出来，只能测 | 品玩是非法 XML、HN 查询信噪比为零、arXiv 单次 634 条 |
| **② 机械校验**（测试断言 + 逐字节比对） | 6 项 | 它不依赖我的判断力，也不疲劳 | `test_replay_is_idempotent` 断言失败，揪出「窗口外条目反复计为新增」 |
| **③ 真的去看结果**（截图 + 本地视觉模型核对） | 5 项 | 静态断言只查「元素生成了吗」，永远不查「页面对不对」 | 月亮图标是空 `<span>`、占位块字母发虚、重点区时间显示两遍 |

**教训**：验证不能停在「DOM 里生成了元素」。三类手段缺一不可 —— 只探针会漏掉自己代码的错，
只测试会漏掉「测试没想到要断言的东西」，只靠肉眼会漏掉隐蔽的数值与幂等缺陷。

> 8.3 的 7 处缺陷中，5 处是第 ③ 类发现的；第三节的 6 项里 6 项是第 ② 类发现的。

### 3.1 InfoQ 中文站时区标注错误（最隐蔽）

- **现象**：报告里「最新条目」的时间**比运行时刻还晚** —— 一条来自未来的新闻。
- **第一反应是错的**：先怀疑自己的解析代码，把 5 种日期格式逐个验证 —— 全部正确。**这条路是死的。**
- **正确定位**：把 feed 里最新一条 pubDate 与**真实系统时间**对照，
  发现源站写的是 `19:25:24 GMT`，而真实 UTC 是 `11:25:24`、北京时间恰好是 `19:25:24`。
  即**源站把北京时间标成了 GMT**，整源时间偏早 8 小时。
- **为什么难发现**：它伪装成解析 bug，会先把你引到自己的代码里；只要不去和「真实当前时间」对照，
  这个错误永远不会自己暴露。
- **解决**：源级配置 `declared_utc_offset_minutes=480`，保留钟面时间按北京时间重新解释。
  关键细节：更正必须发生在「未来时间」判定**之前**，否则整源条目会先被当未来时间丢弃，
  更正逻辑永远走不到。
- **防复发**：顺手写了 `tools/audit_sources.py`，用「源站声称的时间是否晚于真实当下」
  这一条判据**扫描全部源** —— 结论是 16 个源里只有 InfoQ 有问题
  （量子位的 `+0000` 经核实是正确的）。**把一次排查变成一条常态检查。**

### 3.2 事件聚合把 4 篇无关论文并成一簇

- **现象**：一篇 arXiv 论文被打上「跨源事件」标记，且 4 篇论文同簇。
- **定位**：写诊断脚本打印每一对标题的相似度、共享实体与实体全集。
  一眼看到共享实体是 `{'Adaptive', 'An'}` —— **冠词 `An` 漏进了实体集**（停用词表漏项），
  而 `Agents` / `LLM` / `Adaptive` 这些通用名词被当成了区分性实体。
- **为什么严重**：这些论文凭**虚假的「多来源报道」**拿到了跨源加分，挤进了「今日重点」——
  即打分被上游的脏聚类污染了。这不是「聚合不准」，而是**错误一路传到了产品结论**。
- **解决**：分三层 —— 补停用词表（冠词 + 通用技术名词）、
  加**文档频率过滤**（窗口内太常见的实体自动淘汰，是对静态黑名单的动态补充）、
  要求 ≥2 个共享稀有实体。误聚从 8 簇降到 3 簇。
- **沉淀**：这 4 篇标题作为**回归用例**进了 `tests/test_dedupe.py` 与 `tests/test_cluster.py`；
  测试里刻意把来源交替分配，以隔离出真正被测的那一层（避免「同源不成簇」规则掩盖实体逻辑）。

### 3.3 InfoQ 摘要是纯导航链接

- **现象**：前端第一张卡片渲染出 `点击查看原文>`。**这是「真的去看页面」发现的**，测试全绿。
- **定位**：InfoQ 的 `<description>` 里只有 `<div align=right><a>点击查看原文></a></div>`，没有任何正文。
- **量化**：写脚本扫全部源：279 条里 35 条（12.5%）摘要为空或极短，其中 InfoQ 是 **20/20 全中**。
  量化同时确定了阈值（`SUMMARY_MIN_CHARS=20`），并避免过度过滤 ——
  量子位有些 30 字左右的摘要其实是正常短讯，不该一刀切。
- **解决**：`clean_summary()` 在**解析层**就置空，而不是让前端去判断。

### 3.4 窗口外条目反复计为「新增」

- **现象**：报告写「新增 N 条」但「存储共 M 条」，差几条；第二次运行同一批数据**仍然是「新增 5 条」**，
  破坏了幂等性在报告层面的成立。
- **定位**：这 5 条发布时间早于 30 天窗口，每轮被合并进去、又立刻被 retention 淘汰，
  下一轮又因为不在存储里而被当成新条目。
- **怎么发现的**：**是被测试逼出来的** —— `test_replay_is_idempotent` 断言 `total_new == 0` 失败。
  肉眼绝对看不出来：界面正常、数据正常，只有那个数字不对。
- **解决**：`split_by_window()` 在合并**前**丢弃窗口外条目并单独计数。

### 3.5 其他小问题（机械比对揪出的 6 项）

| 问题 | 怎么发现 | 修复 |
| --- | --- | --- |
| `html_to_text` 没折叠换行，`a\n\nb` 压成 `a   b` | 单元测试断言失败 | 空白正则纳入 `\r\n` |
| `https://a.com` 与 `https://a.com/` 归一结果不同 → 会被导入两次 | 单元测试断言失败 | 站点根统一归一为 `/` |
| 重复判定对「标题加来源后缀」漏判（包含度 1.0 但 Jaccard 仅 0.84） | 单元测试断言失败 | 改用包含度 + 长度比护栏 |
| `--now` 冻结时刻时 `run_id` 仍带随机后缀 → 重放产物不可复现 | 逐字节比对失败 | 由输入指纹派生 `run_id` |
| 每源 `duration_ms` 是墙钟耗时 → 重放产物不可复现 | 逐字节比对失败 | 冻结时刻时清零 |
| Google AI Blog 大陆直连 TLS 超时 | 报告显示 `failed(timeout)` | **不改代码**：这是网络路径问题。加 `needs_overseas_network` 标注并写进「已知限制」 |

这里最后一条同样重要：**判断「哪些不是 bug」和判断「哪些是 bug」一样需要证据。**
它后来被验证为间歇性失败（云端境外出口下同一源成功返回），因此没有去写降级代码。

### 3.6 体验改造轮次引入的 7 处缺陷

四项界面改造（等高卡片 + 配图、明暗切换、源名英文 + 语言标签、筛选指示与一键清除）
中，第 1 项**必须动管线** —— 数据里原本没有任何配图字段。这一轮共暴露 7 处缺陷：

| 问题 | 怎么发现 | 修复 |
| --- | --- | --- |
| 配图实际在 `<content:encoded>` 里，实现却只扫 `description` | 真实样本断言「取到 0 张图」 | 新增合并取数函数；爱范儿 / 钛媒体 / The Verge 的配图从 0 恢复到 5 / 5 / 10 |
| 懒加载标签里 `src` 是占位图，真图在 `data-original` | 自写的合成用例断言失败 | 改为先切出 `<img>` 标签，再按属性优先级取值 |
| 源展示名只在本轮抓到的条目上更新 → 19 条露出中文源名 | 截图核对 | 改为对**全部在库条目**按配置重派生（与「本轮抓没抓到」解耦） |
| 重点条目上时间显示两遍（`reasons` 里本就带一句相对时间） | 截图核对 | 合成展示理由时剔除该时间项 —— 用同一函数产出的字符串比对，不靠猜时间格式 |
| 健康度把官方源数量标成「覆盖来源」 | 截图核对 | 标签与筛选条统一为「官方源」 |
| 主题按钮的月亮图标写成空 `<span>`（深色下按钮一片空白） | 截图核对 | 换成内联 SVG，并补「新会话仍记得主题」的独立校验 |
| 占位块字母在浅色主题下发虚 | 截图核对 | 字母改用主题前景色，源色改由背景淡底承载 |

另有**两次我自己的补丁失误**被机械校验拦住：CSS 自定义属性 `--fg-faint` 在同一主题块里
被写成两行（后一条静默生效，肉眼看不出来）。为此在校验脚本里加了「同一主题块内不允许
重复自定义属性声明」这项断言。

**本轮 7 处缺陷里有 5 处是「截图核对」发现的** —— 当时静态断言全绿，页面实际是错的。
这是这一轮最值得记的教训。本轮完整交代见第八节。

## 四、侦察阶段的实际产出

侦察阶段换来了这些**可提前决策的事实**。它们的共同点是：**如果留到后期才发现，
要推翻的不是一行代码，而是数据模型或产品口径。**

| 发现 | 若不提前发现的后果 |
| --- | --- |
| HN `query=AI` 按日期查询信噪比为零（20/20 条 <10 分） | 「热度排序」功能整体失效，且到后期才发现要重做查询策略 |
| arXiv 单次 634 条 / 1.34MB，与「近 7 天」在数学上冲突 | 存储模型与筛选口径需推翻重做 |
| OpenAI 单 feed 1193 条 | 首次导入灌满存储 |
| 机器之心/36氪/Anthropic 等 7 个源已失效 | 把无效源写进配置，运行时才发现 |
| 品玩是非法 XML | 可能改用 `xml.etree` 从而丢掉整个源（选了 feedparser 才容错） |
| 品玩类源与 VentureBeat 的停更形态 | 「停更」与「抓取失败」会被混为一谈 |
| InfoQ 时区错误 | 所有时间筛选与展示都偏 8 小时 |

其中「arXiv 与近 7 天的数学冲突」和「HN 信噪比为零」这两项，**改动了数据模型与产品口径**，
属于前期不定后期推翻的类别。**这是「先探针、后配置」最直接的回报。**

## 五、取舍顺序：时间不够时先砍什么

在既定投入内按要求取舍，按下表顺序砍，**先砍的排在最前面**：

| 顺序 | 砍什么 | 影响 |
| --- | --- | --- |
| 1 | AI 摘要生成 | 唯一引入外部依赖与密钥的功能，砍掉不影响其余 |
| 2 | 事件聚合 | 失去「多来源报道」标记，其余功能完整 |
| 3 | 「今日重点」区 | 退回纯列表，仍有筛选/搜索/来源口径 |
| 4 | 来源色相编码 | 视觉锚点减弱，信息结构不变 |
| 5 | 离线重放样本 | 失去确定性验收手段 |

**不砍**：去重与幂等、时间如实性、单文件可打开、源失败隔离与报告。

实际执行结果：**第 1 项（AI 摘要）就是按这个顺序砍掉的**，保留在「未完成」清单里并写明理由
（见 `VERIFICATION.md` 第 3 节），而不是悄悄不做。其余 4 项都保住了。

## 六、与实施计划的偏差

| 计划 | 实际 | 原因 |
| --- | --- | --- |
| 前端用原生 HTML/CSS/JS | 一致 | 零构建方案在复核阶段省掉了整条 node 工具链 |
| 「把数据内联进 HTML」只是备选 | **提升为必做** | 计划里已识别「双击因 CORS 打不开」，实测确认后它变成交付硬要求 |
| 摘要清洗只做截断 | 增加了样板文案判定 | 实测发现 InfoQ 100% 是导航链接 |
| 打分用单一阈值选重点 | 改成 4 条规则 | 单一阈值无法向用户解释「为什么这条是重点」 |
| 只保留 16 个生产源 | 增加 1 个留档源 | VentureBeat 的停更形态是有价值的回归样本，删掉配置会让经验消失 |
| 离线样本只挑小源 | 扩展到全部生产源 | 只有全量重放才能验证整条链路，含 4 类日期格式与两个非 feed 适配器 |

## 七、可复现性声明

本文件中的每个断言都可由仓库内命令复现：

```bash
python tools/audit_sources.py                    # 源站审计（第四节）
python tools/audit_sources.py --json audit.json  # 含 sha256 与时间可信度判定
python -m pytest tests/ -q                       # 205 项，含全部回归用例（第三节）
python tools/verify_ui.py                        # 前端 99 项：无头 Chrome 真实渲染 + 真实点击
python collect.py --input tests/fixtures/replay --now 2026-09-16T12:00:00Z --data-dir /tmp/x
```

`data/runs.json` 保留了**开发期间的全部运行记录**（含修复前的「新增 5 条」幽灵新增、
Google 源不可达期间的 `partial`）。这些记录未做清理 —— 它们是上面每条排查结论的原始证据。

## 八、第二版：体验改造（今日，计入上述 6 小时）

> 本节回答三个问题：**今天做了什么**、**项目起步时遇到什么问题**、**第一版之后遇到什么问题、怎么改善的**。

### 8.1 今日进度

四项体验改造，对应测试题「视觉设计与使用体验」（25 分）与「产品判断与小巧思」（15 分）：

| # | 改动 | 落地位置 |
| --- | --- | --- |
| 1 | **卡片等高 + 有图显图 / 无图占位** | `web/index.html` + 管线新增配图字段 |
| 2 | **明暗模式切换**（记住用户选择，未选过时跟随系统） | `web/index.html`：CSS 变量 + `localStorage` |
| 3 | **源名统一英文 + 界面保持中文 + 卡片语言徽标（可点筛选）** | `pipeline/config.py` 新增 `name_en`，前端全量改用 |
| 4 | **筛选条件指示 + 一键清除 + 今日重点标签可点击** | `web/index.html` |

此外这一轮还完成了 **GitHub 建仓 → 推送 → Pages 上线 → 云端首次运行**（11 步全绿），
以及三份交付文档（`README` / `VERIFICATION` / `SUBMISSION`）。

第 1 项**必须动后端**：数据里原本没有任何图片字段（全仓搜索 `image|thumbnail|enclosure`
零匹配）。这里的取舍见 8.3。

### 8.2 项目起步阶段遇到的问题

完整清单在第四节与 3.1–3.5。这里只列**一开始就撞上、并且改变了方案**的那些：

| 起步时的问题 | 怎么发现的 | 对方案的改变 |
| --- | --- | --- |
| **7 个候选源已失效**（机器之心 / 36氪 / Anthropic / DeepMind / DeepLearning.AI 等） | 逐个实测，而不是照抄推荐清单 | 源清单只保留实测返回 200 的；把「先探针、后配置」定为纪律 |
| **品玩的 feed 是非法 XML** | 抓下来喂给解析器才暴露 | 依赖选择被改变：保留 `feedparser`（非法 XML 也能救回条目），不用 `xml.etree`（会丢掉整个源） |
| **InfoQ 中文站把北京时间标成 GMT** | 报告里「最新条目」比运行时刻还晚 | 引入源级 `declared_utc_offset_minutes`，并写 `tools/audit_sources.py` 把 16 个源全扫一遍 |
| **HN `query=AI` 信噪比为零**（20/20 条 < 10 分） | 实测 points 分布 | 「热度排序」差点整体失效 → 改双轨查询 + 数值过滤 |
| **arXiv 单次 634 条 / 1.34MB，与「近 7 天」在数学上冲突** | 实测响应体量 | 存储模型与筛选口径被迫提前决定（限流 + 只留 `new`/`replace`） |
| **双击 `index.html` 白屏** | 真的去双击 | 「数据内联进单文件」从备选**提升为必做** |

**起步阶段最大的教训**：源站给的数据不能直接拿来用；**配置必须来自实测探针输出，
不能凭记忆或文档填写**。这条纪律后面一直在兑现 —— 本轮新增的 `name_en` 与配图字段，
同样先跑离线重放统计样本，再决定实现。

### 8.3 第一版之后遇到的问题，以及怎么改善

第一版（能跑、能看、205 项测试全绿）交付后，**自己看页面**发现了问题。
这些是静态断言抓不到的，因为断言只检查「元素有没有生成」，不检查「页面对不对」：

| 第一版之后的问题 | 为什么测试没抓到 | 怎么改善 |
| --- | --- | --- |
| 卡片高度参差不齐 | 断言不查几何尺寸 | grid 拉伸 + 缩略图盒子固定 96×72（有无配图尺寸一致）+ 标题/摘要各截断 2 行 |
| 数据里没有配图，且「不抓文章配图」是既定取舍 | 该功能本就不存在 | 只从 feed **已下载**的字段提取（`media:content` / `media:thumbnail` / `enclosure` / 正文首张 `<img>`）—— **零额外请求**，保住原取舍 |
| 源名中英混杂 | — | 配置新增 `name_en`，前端全部改用英文名，界面文案保持中文 |
| 筛选之后不知道自己在筛什么 | — | 新增「当前筛选」条：逐条可单独移除 + 「清除全部」 |
| 重点条目上时间显示两遍 | 断言不查文案重复 | 根因是 `score.py` 把相对时间也塞进了 `reasons`；改为**用同一函数产出的字符串**比对剔除（不靠猜时间格式） |
| 19 条条目露出中文源名 | 断言不查「这条的展示名哪来的」 | 展示名改为对**全部在库条目**按配置重派生，与「本轮抓没抓到」解耦 |
| 主题按钮的月亮图标是空 `<span>` | — | 补内联 SVG |
| 占位块首字母在浅色主题下发虚 | 断言不查对比度 | 字母改用主题前景色变量，源色改由背景淡底承载 |
| 健康度把官方源数量标成「覆盖来源」 | — | 标签与筛选条口径统一 |

**最关键的一次改善：不再只信静态断言，新增了「真的去点」的校验。**
`tools/verify_ui.py` 用无头 Chrome 渲染真实页面后，**真的去点**筛选标签、来源、语言徽标、
主题按钮与「清除全部」，断言**筛出来的集合**是否正确（而不是只看 DOM 里有元素）。
它第一次运行就抓出 3 个真 bug；随后又补了「新会话是否仍记得主题」的独立校验。

改善后的数字：测试 164 → **205 项**；前端点击校验 **39/39**；
配图提取 0 → **64/234 条（27.4%）**；配图提取的 bug 修完后，样本覆盖率 18% → 25.5%。

（配图口径会随每日更新变化：云端首次运行在境外出口抓到了 Google AI Blog 的 15 条**全部带图**，
覆盖率因此上升。属设计预期。）

### 8.4 两个关键决策（测试题要求列出）

**决策一 —— 静态站 + 云端定时任务，而不是服务端应用。**

- 理由：要求「不依赖用户打开网页或个人电脑持续开机」。GitHub Actions 每日 UTC 22:00
  （北京 06:00）采集后提交 `data/*.json`，Pages 托管构建产物。零服务器、零运行时成本，
  且每次数据变更天然留在 git 历史里，本身就是「定时任务真的在跑」的证据。
- 代价：做不了实时搜索与分页，只能把数据内联进页面（234 条约 382 KB，单文件，可接受）。

**决策二 —— 去重是「删除」，聚合是「建立关系」，两者必须分开。**

- 理由：同一事件被 5 家媒体报道时，5 条**都必须保留**（这正是「多个来源」的证据），
  只是互相标记为同簇。若把聚合实现成删除，就丢掉了产品最核心的「多来源互证」信号。
- 代价：必须保证簇 id 稳定、同源不成簇，并给聚合加精度护栏（≥2 个共享稀有实体 +
  文档频率过滤），否则会像首版那样把 4 篇无关论文并成一簇、让它们凭虚假的
  「多来源报道」挤进重点区。

### 8.5 主要开发工具与 AI 使用情况（测试题要求注明）

- **语言/运行时**：Python 3.11（项目内虚拟环境 `.venv`）
- **运行时依赖仅 4 个**：`httpx`、`feedparser`、`pydantic`、`python-dateutil`；测试用 `pytest`
- **前端**：原生 HTML/CSS/JS，**零构建、零依赖** —— 未使用任何组件库、CSS 框架或页面模板
- **引用的开源项目**：无。未抄用现成模板或开源站点，只使用上述库
- **AI 编程工具**：使用 **Hermes Agent（模型 DeepSeek V4）** 作为结对工具，
  承担代码生成、缺陷排查、测试编写与文档撰写。
  - **AI 的实际作用点**：模块脚手架、逐源探针脚本、测试用例、文档初稿；
  - **AI 出错、被拦下的地方**（如实记录）：3.6 节里两次 CSS 自定义属性重复声明、
    一次断言写反（`-webkit-line-clamp` 截断时 `scrollHeight > clientHeight` 是**正常现象**，
    原判据原理相反）、一次读图误报（把 `Leiphone` 读成 `Lefiphone`）；
  - **截图核对由本地视觉模型辅助完成**（Ollama `qwen2.5vl:7b`，离线、零成本）——
    8.3 表里 9 项问题中有 5 项是它先看出来的；它的误报也一并保留在上面，未做美化；
  - **纪律**：所有源 URL、字段与阈值都来自实测探针输出，**没有一条是凭记忆填写的**。

### 8.6 收尾轮：四个问题的定位与解决（同日，仍计入 6 小时）

**① 一轮采集 16 个源全部失败 —— 环境变量里留着一个没人在听的代理**

- **现象**：某轮 16 个源全是 `WinError 10061`，原始 / 保留 / 新增都是 0。
- **定位**：环境变量里设着 `HTTPS_PROXY=http://127.0.0.1:7897`，而该端口当时没有监听（Clash 没在跑），
  请求全撞在代理上。
- **解决**：先核数据完整性（`news.json`、`meta.json` 的 5 条重点都**没被写坏**）→ 回滚 `data/` 改动 →
  `unset HTTPS_PROXY HTTP_PROXY https_proxy http_proxy` 后直连重抓：14 个源 HTTP 200。
- **顺带解决一件对不上的事**：云端那次运行（出口在境外，能连上 Google）也往 `main` 推过一份数据，
  与本地这份**各有一两条互不重叠**。按管线「数据只增不减」的本意合并：以云端那份为基、本地直连再跑一轮，
  最终库中 **276 条**（合并前两边分别是 265 / 266 条）。
- **顺带发现的缺陷**：全失败时 `meta.json` 会被写空，「今日重点」随之消失。已记入 README 已知限制第 3 条；
  修法留待下一轮（本轮不动 `pipeline/`）。

**② 一个信源反复抓取失败 —— 不是墙，也不是代理，是地址层**

- **逐层拆解**：DNS 解析 211ms、TCP+TLS 握手 437ms 都正常 → 直连确实拿到 HTTP 200，但**首字节 21 秒**、
  复测 1.0 秒 → 该 feed 自己 301 跳到新路径 → 把 4 个 A 记录逐个拆开试：2 个正常（整轮约 0.85 秒），
  `216.239.34.21` **TCP 握手通过但 TLS 卡死不返回**，`216.239.36.21` 连 TCP 都不通。
- **为什么耗满 65 秒**：HTTP 客户端一旦在 TCP 层连上，就不会因为 TLS 卡死而回退到下一个地址，
  于是每次尝试耗完 20 秒超时、3 次重试加退避 ≈ 63 秒 —— 与 `data/runs.json` 里 7 次失败实测的
  64.5–65.8 秒对上。
- **结论**：这个源成不成，取决于当轮 DNS 把哪个地址排在前面；7 次失败**全部**来自这一个源，
  源级隔离保证其余 15 个源与已入库数据不受影响。修法（地址级回退）留待下一轮，README 已如实写明。
- **一处自我更正**：中间的临时统计脚本把 `no_new` 算成了失败，得出「17 轮失败 10 轮」；改用
  `data/runs.json` 逐条核对，实际是该源 **11 轮成功 / 7 轮失败**（18 轮）。文档数字一律以仓库数据为准。

**③ 「今日重点」右栏底部攒出一整块空白**

- **现象**：右侧小条目按内容自然高度排，排完离卡片底边还差 241px，留出一整块纯背景。
- **解决**：侧栏改为按**特稿高度等分**、条目内容垂直居中（不是靠挪条目去填）。
  实测空洞 **241px → 0px**，侧栏与特稿 **867 = 867px**，余量改为在 4 条之间上下对称分摊。
- **护栏**：「右栏底部不留空洞」写成**量几何**的断言，以后改样式不会悄悄退化。

**④ 未读正文太淡，读起来费劲**

- **现象**：为做层次把正文整体调淡，结果**没读过的**正文也发灰。
- **解决**：把「淡」从通用正文移到**已读态**：未读用提对比度的令牌（深色 8.23:1、浅色 10.33:1），
  只有已读才降不透明度（`opacity .55`）。两条断言钉住：未读对比度 ≥ 7:1、初始卡片不得已读样式。

**收尾的两个小动作**：「重要度评分」后的顿号改成冒号（它后面跟的是「用什么算的」）；改完发现套件原来
只断言子串「重要度评分」，**标点改了不会红** —— 于是把整句字面钉进套件，前端断言由 39 项增至 **89 项**。

**上线替换**：初版是最早推送的版本，线上跑了一整天；本轮把第二版推上 `main`，由 GitHub Pages 重新部署
替代初版。初版截图保留在 README「在线版本沿革」里并注明初版，便于对照。

**上线结果**：线上已换成第二版 —— 实测 `https://oi1784105-spec.github.io/ai-intel-station/` 返回
**516,581 字节**、`sha256:1e936ea5c68d8184`，与本地 `dist/index.html` **字节级一致**（面试官下载的那个 html
就是线上那份）；页面上「重要度评分」后已是冒号、顿号版消失。线上当前共 **299 条**（云端 workflow 那轮又采了 23 条）。

**一处按实情更正的旧说法**：核对运行记录时发现 `schedule` 事件**已经自然触发过一次并成功**
（GitHub run `35165566489`，UTC 2026-09-17T00:12，11 步全绿、Pages 部署成功；比配置时刻 UTC 22:00 晚约 2 小时，
属 GitHub 调度排队）。原先文档里写的「定时触发尚未验证」是按当时的实情写的，现在改为
「已自然触发一次成功；连续多日是否准点，交付期内仍无法观察」。

### 8.7 第三版：手机端版式（同日，仍计入上述 6 小时）

**起因只有一句话**：自己在手机上打开线上页 —— 从报头到卡片，**右侧一整截全被裁掉**，页面还能横向拖。
桌面端是好的，所以这个问题一直没被发现：所有前端校验都在 1500px 视口上跑，窄屏根本不进检测范围。

**① 先量，不先猜**。写了个几何探针，在 390px 视口里量真实数值：

- 文档宽 **549px**，视口只有 390px —— 横向溢出 **159px**，这就是「右侧一截被裁」。
- 越界元素锁定在「今日重点」：`lead-hero` 宽 **531px**，而它的容器只有 348px。
- 消融实验（逐个隐藏候选元素再量宽度）把元凶钉在 **「同一事件的其他来源报道」那一条**：
  隐藏它，hero 立刻回落到 346px、文档宽回到 380px。

**② 根因不是「宽度写死了」，是 CSS 的固有尺寸规则**。那条里的标题是
`white-space:nowrap` + 省略号，它作为**网格项**会把 `1fr` 轨道的**自动最小值**（min-content）
顶到 531px —— `1fr` 不等于「可以随便缩」，轨道的最小值取决于内容。所以卡片列表没坏（`.card-txt`
早就写了 `min-width:0`），同一个项目里「今日重点」却漏了这一步。处方就是补齐它：给网格项显式
`min-width:0`，省略号机制才能接管。**同类问题一并查了兄弟路径**：数据面板的源表是
`table-layout:auto`，展开时把整页顶到 422px，让它在面板内部自己横向滚动。

**③ 顺手把「手指」和「鼠标」分开看**。桌面上 17–20px 高的文字链接给鼠标够用，手指不行：
手机端最细的可点控件是「语言」筛选，只有 **11×10px**；「只看未读」23px。窄屏统一给到
≥32px（图标按钮 38px），一共 **386 个 → 0 个** 不达标。这条规则放在 `max-width:1023px`
而非 `640px`，因为它是「手指 vs 鼠标」的问题，平板同样需要。

**④ 报头与区块标题**。报头在 390px 里挤了三样东西，还把桌面快捷键提示「Ctrl K」也带上去 ——
触屏上没有意义，还挤掉了输入框的占位文字（只能显示「检索核…」）。窄屏隐藏该提示、搜索框吃掉
剩余宽度，粘性报头高 **55px**，不抢屏。区块标题原本是「今日重点 —— 右对齐英文小标签」，
小标签 197px 不换行，把左侧说明文字挤成 **145px** 宽的窄条（74px 高、5 行），改为竖排后
说明文字拿满 **358px**。

**⑤ 怎么证明「电脑端一点没动」**。这不是靠眼看：所有改动都落在 `max-width:1023px` 与
`640px` 两段里，然后拿 **1024 / 1280 / 1500 三个宽度 × 深浅两个主题共 6 张截图**，
改动前后做 **SHA-256 比对 —— 6 张全部逐字节相同**。改前基准图是从 git 里的上一版模板
用同一个 `build_site.py` 重新构建的（构建结果哈希 `1e936ea5c68d`，与线上一致，说明基准就是线上那份）。

**这一步中间还起过一次疑**：某次比对报「6 张全不同」，而前几次都是相同。查下来是**两个复现条件**
没控住，而不是 CSS：① 页面上有「14 小时前」这类**按当前时间现算、小时级**的文案，前后两次渲染
跨了小时边界就会变一个字符（实测差异只有元数据行里 9×9 像素）；② 两次渲染若共用同一个浏览器
profile，前一次写进 localStorage 的主题会被后一次读走。改成**同分钟渲染 + 各自独立 profile** 后
复测，**差异像素 = 0**。这两条都写进了 `VERIFICATION.md` 1.6.1 的复现提示里 —— 不然别人照着做会
得出「桌面被改坏了」的结论。

**⑥ 把它钉成回归**。`tools/verify_ui.py` 增加第三段：**390px 手机段，10 条断言**，
前端断言由 89 项增至 **99 项**。取数上踩了两个坑，都写进了 `VERIFICATION.md` 1.6.1：

- headless Chrome 的窗口宽度**最小只有 512px**（换 flags 也绕不过；`--force-device-scale-factor`
  只改设备像素比、不改 CSS 宽度）。真正的 390px 视口要靠 **390px 宽的 iframe 内嵌**才拿得到。
- 探针一开始写成 `async` + `setTimeout`，结果**报告时有时无**：外框页没有待办任务时
  `--virtual-time-budget` 会立刻耗尽，`--dump-dom` 在探针写完之前就抓走了。
  改成把探针挂在 iframe 的 `load` 里同步测量、结果挂到全局，由外框页在自己的 `load` 里读走
  （外框 load 必然晚于 iframe），就不再和时间赌。**同一个坑当天踩了两次**（第一次是探针写进
  iframe 内部导致 `--dump-dom` 读不到），教训记在此处。

**⑦ 一处自我纠正**：中途把卡片缩略图从 108×80 缩到 92×68 想给标题多让点宽度，复核时看渲染结果
发现缩略图小到看不清、反而更弱，于是**收回这条改动**，只保留「卡片内距 20px→16px」的收益。
写在日志里是因为它正说明为什么这类改动要**边改边看渲染结果**，而不是一次改完再看。

**⑧ 上线与数字复核**。第三版推上 `main`（`be9cfbe`）后，由 `workflow_dispatch` 触发 `update.yml`
替换线上页（GitHub run `35227962891`，success，约 65 秒 —— 这个 workflow 同时负责采集与部署，
推 `main` 本身不会换线上页）。这一轮的 CI 采集顺手采到 **90 条新条目**（299 → **389** 条），
所以文档里所有与数据相关的数字**都在这一轮之后用脚本重算过**，不是沿用旧值：

| 指标 | 第二版时 | 本次复算 |
| --- | --- | --- |
| 库中条目 | 299 | **389**（中文 180 / 英文 209） |
| 配图率 | 25.4%（76/299） | **25.7%（100/389）**，来自 9 个源 |
| 近 7 天运行成功率（轮次口径） | 0.6316（12/19） | **0.65（13/20）** |
| 源级成功率（源次口径） | 297/304 = 97.7% | **313/320 = 97.8%**（7 次失败仍全部来自 `google-ai`） |
| 产物 / 线上 | 516,581 字节 | **620,486 字节** / `sha256:f5554500b8adbbe9`，与线上**逐字节一致** |

CI 这一轮自身健康：11 源 `ok`、5 源 `no_new`、0 源失败（`data/update-status.json`）。

**⑨ 又一个网络坑**：本轮 `git push` 与 `git pull` 连续撞 `github.com:443` 连接重置，
但同一时刻 `api.github.com` 与 Pages 站点都正常。换 `git -c http.version=HTTP/1.1` 一次通过
（没深究原因，记下来备用）。**数据也因此差点没拉回来** —— 部署后若不同步 CI 提交的数据，
本地就不会再等于线上，「字节级一致」这句话当场失效。

### 8.8 第四版：报头与手机字号（同日，仍计入上述 6 小时）

**起因只有一句话**：真机截图里报头「每日 AI 情报站」和搜索框**贴在一起、像压住了**，头条字大得像一堵墙。

**① 先定真机宽度，不猜**。截图 1179×2556 —— iPhone 的 3 倍图，1179 ÷ 3 = **393px**。
上一版只在 390px 上验过，宽度差 3px 不是问题所在；真正的问题是**旧断言查不出这种坏**。

**② 量报头几何，拿到铁证**。站名右缘 **135.2px**、搜索框左缘 **123.1px** —— **水平重叠 12.1px，
矩形相交**。根因是 `.mast-left{min-width:0}` 允许左栏被压扁，而里面的 `.brand{flex-shrink:0}`
拒绝收缩：**内容溢出自己的盒子照样绘制**，于是站名画到了输入框上面。这类缺陷用「有没有元素越过
屏幕右缘」永远查不出来 —— 它没越界，是**兄弟元素互相压**。处方是窄屏报头改两行（站名一行、
搜索框整行 + 图标），左右两侧不再争同一行的宽度，构造上不可能再压；并把「元素中心点**命中测试**」
写成断言：谁被压住就点不到，测试立刻红。报头高度仍压在 **79px ≤ 80px**（站名行 32px 是触控下限
不能动，就收内距、行距，图标按钮 38 → 36px）。

**③ 字号另立一套**。窄屏量出来头条 **22px / 行高 1.28**、区块标题 **24px 比站名 18px 还大**
（层级倒挂）。桌面那套是给宽栏排的，搬到 358px 栏宽里就成了「巨字密排」。窄屏改为头条
**19px / 1.45**、区块标题 20px、卡片正文 13px / 1.75。

**④ 顺手揪出两条「写了但从未生效」的 CSS**。头条正文的规则选择器是 `.lead-hero .sum`，
而 JS 渲染出的真实类名是 `lead-sum`；元信息行同理（CSS 写 `.lead-no`，JS 给的是 `lead-meta`）。
所以头条正文一直**继承 body 的 13px 无衬线**，元信息也没拿到等宽小字。按真实类名重写，
并把「头条正文 13.5–15.5px 衬线」钉成断言。这类「CSS 存在 ≠ 生效」的缺陷，
只有把**渲染后的计算值**读出来才会暴露 —— 靠读源码看不出来，两处类名都写得好好的。

**⑤ 改完又冒出一个 360px 溢出**。卡片正文提到 13px 之后，360px 视口整页溢出 9px：量出来
`.cards` 容器 328px，而网格轨道是 **352px** —— 卡片里是 `-webkit-line-clamp` 的收缩盒，
它算出的 min-content（352px）成了 `1fr` 轨道的自动最小值。改 `minmax(0,1fr)` 把自动最小值钉死为 0，
并把同类的 `1fr` 网格（`.lead-mini` / `.cards`）一并兜住。同时把手机段从 1 档扩到
**2 档（360px / 390px）** —— 上一版只验 390px，正好漏掉这个缺陷。

**⑥ 证明「电脑端零变化」时，差点交出一份假的「全绿」**。比对脚本沿用上一轮写法（浅色不注入、
深色才种 localStorage），跑出来 6 组全相同 —— 但**深浅两档的哈希也完全相同**，说明两档其实
渲染成了同一个主题。查下来是无头 Chrome 的 `prefers-color-scheme` **默认就是深色**。
改成两档都显式种主题，并让脚本**自证**「同一宽度下深浅两档哈希必须不同」，这组比较才算数。
（教训：验证脚本自己也要能被验证 —— 全绿不等于验过。）

**⑦ 一处自我纠正**：「同一事件的其他来源报道」那一列字号从 12px 提到 12.5px 后，标题多折一行、
末行只剩一个字，看渲染结果后**收回**这条改动；只保留给「今日重点」标题区加的那点行距。

**⑧ 上线与数字复核**。第四版推上 `main`（`4cce429`）后，由 `workflow_dispatch` 触发 `update.yml`
替换线上页 —— **推 `main` 本身不会换线上页**，必须触发 workflow（这次是 run `35234905256`，success，
约 90 秒）。这一轮的 CI 采集顺手采到 **6 条新条目**（389 → **395** 条），所以与数据相关的数字
**再次全部重算**，不沿用旧值：

| 指标 | 第三版时 | 第四版复算 |
| --- | --- | --- |
| 库中条目 | 389（中文 180 / 英文 209） | **395**（中文 181 / 英文 214） |
| 配图率 | 25.7%（100/389） | **26.1%（103/395）**，仍来自 9 个源 |
| 跨源聚合（`cluster_size > 1`） | 10 | **6** |
| 近 24 小时 / 近 7 天 | 142 / 271 | **130 / 364** |
| 近 7 天运行成功率（轮次口径） | 0.65（13/20） | **0.6667（14/21）** |
| 源级成功率（源次口径） | 313/320 = 97.8% | **329/336 = 97.9%**（7 次失败仍全部来自 `google-ai`） |
| 产物 / 线上 | 620,486 字节 / `sha256:f5554500b8adbbe9` | **637,593 字节 / `sha256:5a5425fd97b8a0b1`**，与线上**逐字节一致** |

页脚读数同步为「全站共 **395** 条（其中 5 条为重点）」。README / VERIFICATION / SUBMISSION 里
所有引用这些数字的地方都按上表改过。

**⑨ 两个操作层的坑（都不是项目缺陷，但会骗人）**：
① **dispatch 用 urllib 打会 422**（`Invalid request. For 'links/0/schema', nil is not an object.`），
换成 `curl` 或 `gh workflow run` 立刻 204 —— 同一份 token、同一个 body，差别只在 HTTP 客户端；
② 我两个客户端都试了一遍，于是**同一个 workflow 被触发两次**（concurrency 是排队而非取消），
第二个 run 在第 8 步「提交数据变更」失败：第一个 run 已经把数据提交推上去了，第二个没东西可提交、
`git commit` 返回非零。也就是说这个 run **亮红灯但并没有出错**。
要在意的话，把该步骤改成「有变更才提交」（`git diff --cached --quiet || git commit ...`）就能消除误报 ——
本轮没改，如实记在这里。

### 8.9 第五版：窄屏「今日重点」横向轮播（同日，仍计入上述 6 小时）

**起因**：手机上「今日重点」原来是一摞**竖排堆叠**的卡片，不往下翻就不知道一共几条。
要改成横向 Banner 轮播：一次主要看一条、自动轮播、平滑左右滑动、底部有分页指示器、
左右箭头可手动切换（手动后继续自动轮播）、支持手指左右滑动，**只改移动端**。

**① 先定最小实现路径，不引框架、不动 DOM**。窄屏把 `.lead-grid` 变成横向吸附轨道
（`overflow-x:auto` + `scroll-snap-type:x mandatory`），`.lead-side` / `.lead-mini`
这两个**纯包装盒**用 `display:contents` 拆掉 —— 它们的子节点（每张重点卡都带 `.hl`）就直接成了
轨道的一屏。好处是**滑动完全交给浏览器**：手指拖动是原生滚动，不会和卡片上的点击抢事件
（自己用 transform 写轮播最容易栽在这里）；自动播放、左右箭头、指示点都只做同一件事 ——
「滚到第 k 屏」，滑动动画由 `scroll-behavior:smooth` 给。DOM 结构一个节点都不动，
桌面那套 12 栏网格**构造上碰不到**。

**② 第一次探针就抓到一个真 bug**：拆盒后只有头条那一屏拿到宽度（361px），其余 4 屏被压成 **32px**。
原因是 **`display:contents` 只改布局归属、不改选择器匹配树** —— 我写的是子选择器 `.lead-grid>.hl`，
而拆盒后的卡片在 DOM 里仍属于 `.lead-side` / `.lead-mini`，匹配不上。改成后代选择器
`#highlights .hl` 立刻正常。（教训：`display:contents` 是「布局上的拆盒」，不是「选择器上的搬家」。）

**③ 第二个问题是「验不到」，比「改不对」更麻烦**：无头 Chrome 把 `prefers-reduced-motion`
报成 `reduce`，而我原来的实现是「减弱动效就**不自动播放**」—— 于是自动轮播这条路在无头环境里
**永远是关的**，验不到。于是把语义定清楚：**减弱动效只去掉滑动动画（`behavior:"auto"` 一跳到位），
不静默停掉轮播**；并补一个**真能用的暂停/播放按钮**满足 WCAG 2.2.2（对动效敏感的用户自己按停，
按钮标签与 `aria-pressed` 同步）。这样正常档跑 smooth、减弱档跑 auto，两条路都能验。

**④ 第三关是视觉：数值全绿，但「一屏大框里半张白」**。屏高由最高那屏决定，而头条那屏
带着 16:9 大图（520px），纯文字的屏只有 48% 内容 —— 探针的「各屏等高」全绿，
**视觉复核却判定「像图没加载出来」**（和第四版那次「越界断言查不出兄弟元素压字」是同一类：
数字达标 ≠ 看着对）。分三步收：

1. **收起头条的大图**（它是唯一带大图的卡，图一留着整条就被顶到 520px；
   桌面照旧显示大图，手机上这条情报在下方「全部情报」卡片里仍带自己的配图 —— 图不是丢了）；
2. **让矮的屏自己吃掉余量**：摘要 `flex:1 1 auto`（多显示几行正文，末尾仍是省略号，
   不藏信息）、带缩略图的屏让缩略图竖着撑满、标签行用 `margin-top:auto` 钉底
   （和头条「跨源列表」同一招），余量因此集中在一条空档里，而不是把内容浮在正中；
3. 字号在窄屏提一档（卡片标题 15 → 19px），免得在大框里像一行列表。

结果：各屏内容占框高 **90 / 96 / 96 / 96 / 96%**（当轮数据），整条高度从 528px 收到 **512px**
（会随数据浮动几十像素：屏高由最高那屏决定，所以「收住了」是指不再出现半张白，不是某个固定数字）。
并把「每屏内容占框高 ≥75%」写成断言 —— 这条正是上面那次视觉返工的机械复现。

**⑤ 露边的连带问题**：为了「静止时看得出左右还有内容」，屏宽让出 32px、下一屏露 20px 的边。
但这样一来**最后一屏吸不齐左缘**（停在 +32px，比前四屏右缩一截）—— 轨道尾部补上同样的
32px 内距，最后一屏才对齐。断言里因此同时有「露出 8–40px」和「自动播放后当前屏左缘离轨道
左缘 ≤3px」（后者是跑完 19 秒虚拟时间后量的，能证明自动播放确实推进且**吸附对齐**、
不卡在两屏之间）。

**⑥ 三个只在无头环境里遇到的取证陷阱**（都影响「怎么判」，不影响「改得对不对」）：

- **虚拟时间会把平滑滚动一次补完**：滚动的轨迹里只有一个点（`[[1ms, 1119]]`），逐帧的滑动动画
  根本取不到。改用两条机械证据：轨道声明 `scroll-behavior:smooth` + JS 在正常档传
  `behavior:"smooth"`，再拿「减弱动效档轨迹确实是单跳」做对照 —— 后者反过来证明了那个开关真管用。
- **相对时间会让「电脑端零变化」比对无故变红**：相对时间按**每条情报自己的时间戳**在任意分钟
  边界翻档，不钉时钟就会随机差一个 **9×9px 的单字符**（第一次跑就误报 light 1280 不同）。
  给基准页与对照页注入**同一个固定的 `Date.now`** 就消停了。
- **某个淡入过渡会让整块背景差 1 个灰阶**：19.7% 的像素有差异、但幅度**全部 = 1/255** ——
  看着像「不同」，其实不是改动留下的痕迹。比对时禁掉过渡/动画，6 组立刻全部逐字节相同。

**⑦ 门槛与绿**：`tools/verify_ui.py` 手机段新增 20 条 × 2 档、电脑段新增 2 条，
总数 **119 → 161**（161 / 161 通过）；`pytest` **205 项**；`build_site.py --check` 通过；
另有一份**独立临时探针**（不复用 `verify_ui` 的断言，跑完即删）在 393 / 360 两档、
外加「保留 headless 真实 reduce」一组里全部通过，并多验了「暂停后 7 秒内容没有偷偷走」
「19 秒后自动播放确实推进」两条时序断言。桌面 1024 / 1280 / 1500 × 深浅 6 张截图与第四版
**逐字节相同**（6 / 6）。

**⑧ 上线与数字复核**。第五版推上 `main`（`e51efda`）后，由 `workflow_dispatch` 触发 `update.yml`
（run **`35243172713`**，success）—— 顺便记一笔：**推 `main` 本身不会换线上页**
（触发条件是 `schedule` + `workflow_dispatch`，没有 `push`；这次推送只带起了 `check.yml`），
所以「推完不用再手动触发」是个错觉，还是得 dispatch 一次（且只触发一次：第二个 run 会在
「提交数据变更」步骤误报红灯）。这一轮的 CI 采集顺手采到 **3 条新条目**（395 → **398** 条）、
轮次 21 → **22**，于是线上页是**用新数据重建**的：本地按同一份数据重建后与线上**逐字节一致**
（**662,676 字节 / `sha256:014de82ed56d7d38`**）。与数据相关的数字因此**再算一遍**，不沿用旧值：

| 指标 | 第四版时 | 第五版复算 |
| --- | --- | --- |
| 库中条目 | 395（中文 181 / 英文 214） | **398**（中文 183 / 英文 215） |
| 配图率 | 26.1%（103/395） | **26.4%（105/398）**，仍来自 9 个源 |
| 跨源聚合（`cluster_size > 1`） | 6 条 | **6 条 / 3 个事件** |
| 近 24 小时 / 近 7 天 | 130 / 364 | **130 / 366** |
| 近 7 天运行成功率（轮次口径） | 0.6667（14/21） | **0.6818（15/22）** |
| 源级成功率（源次口径） | 329/336 = 97.9% | **345/352 = 98.0%**（7 次失败仍全部来自 `google-ai`：22 轮里 7 成功 / 7 失败 / 8 轮无新条目） |
| 产物 / 线上 | 637,593 字节 / `sha256:5a5425fd97b8a0b1` | **662,676 字节 / `sha256:014de82ed56d7d38`**，与线上**逐字节一致** |

页脚读数同步为「全站共 **398** 条（其中 5 条为重点）」；README / VERIFICATION / SUBMISSION 里
引用这些数字的地方都按上表改过；手机端对照图也按 398 条数据重出了一遍
（对照图的基准页钉在第四版提交 `1ef27ed` 的模板上，不能用 `HEAD` —— 第五版上线后 `HEAD` 已是自己）。

---

## 8.10 第六版：按测试题逐条自检 → 三处对齐

交付前把测试题 §02 作答规则、§03 功能要求（8 个模块）、§05 提交清单、§06 验收与复试
**当成一份验收单**逐条走了一遍 —— 不是重跑一遍测试，而是按验收方会用的姿势去用：
固定输入重复导入、单个来源不可用、缺少日期、超长标题、无图、搜索无结果、随机抽查原文、
以及**按 README 在干净环境跑起来**。脚本一律放 `%TEMP%`、跑完即删，仓库里不留一次性脚本。

**① 干净环境**（§06 明写会查这一条）：新建空 venv → `pip install -r requirements.txt -r requirements-dev.txt`
→ `pytest tests/ -q` **205 passed**；再用同一环境离线重放 `collect.py --input tests/fixtures/replay`
→ 退出码 0、184 条、四个 JSON 齐全；装出来的版本与 `requirements.txt` 一致。这一步的价值在于
**证明 README 里的命令本身是对的**，不是证明我这台机器能跑。

**② 重复导入的两种问法**（§06 + §03-04）：同一输入 + 同一冻结时刻跑两次 → 产物**逐字节相同**；
把产物当历史、再合并同一批输入 → 条目 **184 → 184**，第 3 次运行 `total_new=0 / total_dup=206`。
「不产生重复记录」之外还核了 **id / 归一 URL / 标题三者各自唯一**（只查条目数会漏掉「换了 id
的同一篇」）。

**③ 单源不可用**（§06 + §03-07）：从离线样本里**删掉一个源的文件**再跑 —— 该源 `failed` +
`missing_fixture` 如实上报，其余 15 个源照常跑完，已有 **184 条一条没少**，`overall=partial`
（状态灯 warn）。顺带把「没有新内容 / 解析样本告警 / 疑似停更 / 抓取失败」四种状态各自复现了一次，
确认它们**可区分**而不是混成一个「异常」。

**④ 缺日期与长标题**（§06 点名，线上数据碰不到 —— 434 条发布时间全齐、最长 177 字符）：
按测试题允许的方式用**明确标注的测试数据**演示：在真实数据上追加 1 条标题带
`【验收演示·测试数据】ZZACCEPTDEMO`、`published_at=null` 的条目，用仓库自己的构建器出页，
无头渲染后读回卡片。结论见 `VERIFICATION.md` §1.10；口径也一并写清：**发布时间未知的条目
不进「今天 / 近 7 天」窗口**（无法判定窗口归属），只在「全部」里出现 —— 不写下来就会被当成丢数据。

**⑤ 自检抓出的三处不达标（这就是本轮真正改的东西）**：

| 抓到的 | 根因 | 修法 |
| --- | --- | --- |
| **85 / 398 条卡片只剩一个标题** | 源站 feed 没给摘要，前端「有摘要才渲染摘要行」→ 空着；条款 §03-02 要的是「简短摘要**或来源简介**」 | 按来源 id 加**来源简介**兜底：只写订阅配置里能核实的属性（语言 / 类型 / 是否官方 / 是否做相关性过滤），前缀固定 `来源简介：`，与 `摘要：来源 feed 原文` 显式区分 —— 是「这个源是什么」，不是「这篇文章讲了什么」，不补造事实 |
| 缺时间时写「时间未知」 | 措辞没对齐条款原文（§03-05 写的是「发布时间未知」） | 文案改「发布时间未知」，注释钉死「绝不拿 `first_seen_at` 冒充发布时间」 |
| `.gitignore` 没有 `.env` | 本项目零密钥，所以一直没补 | 补 `.env` / `.env.*` / `*.env` / `secrets.json` —— 防以后随手加 token |
| **窄屏轮播第 2 屏只填 46%**（换到新数据才出现） | 该条重点既没配图、源站也没给摘要，一屏只剩标题 + 一行来源简介；而屏高由「最高那屏」决定，矮屏自己吃不下余量 | 先补**源色占位块**顶住高度，**后按读者反馈撤掉**（那块占位在窄屏像在报警）→ 改走**纯文字**：余量交「摘要 / 来源简介」那一行撑开、来源简介行内垂直居中，各屏仍 **90 / 95 / 96 / 96 / 96%**（并补两条断言钉住）。全过程见 ⑩ |

四处都补了断言（桌面 3 项 + 手机段 2 项 + 二稿 2 项 × 360/390 两档）：**170 / 170 通过**，`pytest 205` 不变，`build_site.py --check` 通过。

**⑥ 这一轮踩的四个「取证坑」**（都跟改得对不对无关，但会让结论失真）：

- **注入脚本用系统默认编码写盘**（Windows 上是 GBK）→ 页里那句中文检索词成了乱码，
  于是「搜不到 / 搜出别的条目」，看起来像搜索坏了，其实是我写文件没指定 `encoding="utf-8"`。
- **无头 Chrome 的视口有 ~512 CSS px 下限**，`--window-size=390` 拿到的是 512 宽的渲染 ——
  要真 390px 得用 iframe 套壳（`verify_ui.py` 手机段一直这么做，这次是临时脚本忘了）。
- **`--screenshot` 只拍视口顶部、不吃 `scrollIntoView`**：想拍列表里某张卡，不是滚动就能拍到；
  最后用「只含那一条标注条目的本地测试页」出图，比裁切整页靠谱。
- **断言写红了先怀疑判据，别急着改产品**：这一轮两次假红都是我的判据错 ——
  ① 同一输入重放时其余源的状态本来就是 `no_new`（不是 `ok`），我却去数 `ok`；
  ② 状态词表漏了 `parse_warning`（离线样本是截断抓取，解析是「报错但已容错」）。
  两次都是探针 bug、产品行为正确；**改判据后重跑**，而不是改代码让红灯变绿。

**⑦ 门槛与绿**：`pytest` **205 passed**、`tools/verify_ui.py` **170 / 170**、
`build_site.py --check` 通过（自包含）；上面 12 项自检全部通过，未通过项为 0。

**⑧ 上线与数字复核**。第六版推上 `main`（`048f1f2` + 复算提交 `bc8d2de`）后，
用 `workflow_dispatch` 触发 `update.yml`（run **`35298248252`**，success）—— 仍然只触发一次
（两个 run 并发会在「提交数据变更」那一步抢提交）。**线上 = 本地重建产物**：`curl` 拉回线上页
**711,730 字节 / `sha256:24a7b417e2e5c43d`**，与本地 `dist/index.html` **逐字节相同**；
线上页里能检到第六版的两处标志串 `来源简介：` 与 `发布时间未知`。

题面 §02 允许「到达时间上限可提交当前成果并说明取舍」，但**数字不能顺手沿用**：这一轮 CI 又采了两轮
（398 → 422 → **432** 条），所以文档里所有数据类数字按仓库自己的口径**重新算了一遍**：

| 指标 | 第五版复算时 | 第六版复算（当前） |
| --- | --- | --- |
| 库中条目 | 398（中文 183 / 英文 215） | **434**（中文 195 / 英文 239；按源语言 198 / 236） |
| 官方一手源条目 | 40 | **43** |
| 带发布时间 / 无发布时间 | 398 / 398（100%）/ 0 | **434 / 434（100%）/ 0** |
| 跨源聚合（`cluster_size > 1`） | 6 条 / 3 个事件 | **6 条** |
| 配图率 | 26.4%（105/398） | **25.3%（110/434）**，仍来自 9 个源 |
| 近 24 小时 / 近 7 天 | 130 / 366 | **126 / 400** |
| 近 7 天运行成功率（轮次口径） | 0.6818（15/22） | **0.72（18/25）** |
| 源级成功率（源次口径） | 345/352 = 98.0% | **393/400 = 98.2%**（7 次失败仍全部来自 `google-ai`：25 轮里 8 成功 / 7 失败 / 10 轮无新条目） |
| 产物 / 线上 | 662,676 字节 / `sha256:014de82ed56d7d38` | **721,691 字节 / `sha256:75572daa8d4cbde0`**，与线上**逐字节一致** |
| 验收自检关注的两项 | — | 无摘要 **94 / 434**（全部有「来源简介」兜底）、无配图 **324 / 434**（列表卡落源色占位块；窄屏轮播改走纯文字、不提示） |

页脚读数同步为「全站共 **434** 条（其中 5 条为重点）」；README / VERIFICATION / SUBMISSION 里
引用这些数字的地方都按上表改过。**仍未验证 / 未完成的口径不变**（连续多日准点、长期运行、并发排队）。

**⑨ 换数据又抓出一个真塌陷**：这一轮的复算不是抄数字 —— 换到新数据重跑 `verify_ui` 时，第五版立的
「窄屏每屏内容占框高 ≥75%」**当场报红**：新数据里出现一条既没配图、源站也没给摘要的重点条目，
在等高的轮播轨道里只剩标题 + 一行来源简介，只填 **46%**（视觉上就是半张白卡）。教训很直白：
**断言是拿来抓数据边界的** —— 这一条要是当初没写下来，这种只在特定数据下出现的塌陷就会一直跟着上线。

**⑩ ⑨ 的第一次修法被真实反馈否掉了（第六版二稿）**：最初的修法是给这类条目补一个**源色占位块**
（来源首字母 + 小字「本条来源未提供配图」，与列表卡 `.thumb.ph` 同一套语言，桌面端 `display:none`），
各屏回到 **89 / 95 / 96 / 96 / 96%**，三条门全绿 —— 但**绿不等于对**。读者把桌面浏览器窗口缩窄
（<1024px，走的就是窄屏那套样式）后看到那块占位，反馈「没有图片的话，只显示文字就好了，不用特意提醒」，
并且说移动端和全面屏都正常、只有缩窄的窗口里这样。

复盘下来两条都成立：① 那块占位在窄屏里**更像在报警**，读者第一眼以为「图挂了」；② 它把
**「维护者需要知道的信息」写在了读者读的地方** —— 「本条来源未提供配图」对读者没有用，
对排查才有点用，而排查有日志和断言，不该占用页面的位置。

二稿因此**撤掉占位块与全部「无配图」提示**（含电脑端图片位那行小字在窄屏的显示），改走**纯文字**：

| 原来 | 现在 |
| --- | --- |
| 窄屏给无图条目摆一个 `.lead-ph` 占位块（源色 + 首字母 + 小字），靠它吃掉余量 | **不摆任何东西**；余量交给「摘要 / 来源简介」那一行自己撑开（`flex:1 1 auto`），来源简介再在行内垂直居中（`align-items:center`）→ 卡片成「元信息 / 标题 + 简介 / 标签」三段分布，**留白就是留白，不是缺件** |
| `.lead-noimg`（「本条无配图（来源 feed 未提供图片地址）」）在窄屏也会显示 | 窄屏 `#highlights .lead-noimg{display:none}`；电脑端照旧（读者说全面屏是正常的，不动） |
| 无 | 新加两条断言钉死这个决定：**窄屏不出现任何「无配图」字样**（0 处）、**无配图的屏靠文字行吃掉余量**（余量高度 ≥ 卡片高 30%） |

各屏仍回到 **90 / 95 / 96 / 96 / 96%**（360px）与 **89 / 95 / 96 / 96 / 96%**（390px），
`verify_ui` **170 / 170**、`pytest 205` 不变。这次还多做了一道取证：把 `HEAD` 里的旧页与改后的页
在 1500px 下**同钟同时**各渲一张截图，**逐字节相同** —— 证明这一改确实只在窄屏生效（详见
`VERIFICATION.md` §1.11）。
