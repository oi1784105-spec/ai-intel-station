# 验证记录（Verification Record）

> 对应测试题「验证说明」。要求：**真实数据获取**、**固定输入重复导入**、**单个来源失败**等场景的验证结果，
> 任务配置及运行记录，并**明确列出「已验证」「未验证」「未完成」**。
>
> 本文所有数字都是**本次实测输出**，不是估计值。每条结论都给出可复现命令。
>
> **关于会变的数字**：库中条目数、配图率、近 24 小时条数等会**随每日自动更新而变化**，
> 本文以两个快照为准 ——
> **v1 快照** = 本地首轮采集（2026-09-16T12:54:19Z）；
> **v2 快照** = 云端首次运行（2026-09-16T13:38:43Z，GitHub Actions）。正文默认给 v2（最新）值。
> 上线后这些数字每天都会变，属设计预期，不是文档过期。
>
> 最后更新：2026-09-16。

## 0. 口径声明（先说清楚每个数字怎么算）

| 项目 | 口径 |
| --- | --- |
| 时间存储 | 一律 UTC；前端按浏览器本地时区显示 |
| `published_at` | **只来自源站自称的发布时间**。解析不出或不采信时置 `null` 并记录原因 |
| `first_seen_at` | 抓取时刻。**绝不用它顶替发布时间** |
| 源级时区更正 | InfoQ 中文站把北京时间标成 `GMT`，用源级 `declared_utc_offset_minutes=480` 更正，并标 `published_note="tz_corrected"` |
| 日期范围 | 滚动 **30 天**窗口（`WINDOW_DAYS=30`），更早条目在合并前丢弃 |
| 「有图」 | 条目带 `image_url`。**仅从 feed 载荷已下载字段提取，零额外请求** |
| 运行环境 A | Windows 10 · Python 3.11 · 中国大陆直连网络（无代理时部分境外源不可达） |
| 运行环境 B | GitHub Actions `ubuntu-latest` · Python 3.11 · 境外出口（无网络限制） |

---

## 1. 已验证（Verified）

### 1.1 真实数据获取 —— 已验证 ✅（两个独立环境各跑一次）

**命令**

```bash
python collect.py --trigger manual          # 本地
# 云端由 .github/workflows/update.yml 执行同一条命令（trigger=cron）
```

**结果**：16 个生产源全部参与。下表为**云端首次运行**（v2 快照）写入库中的实测值；
括号内是**本地首轮**（v1 快照）的值 —— 两个独立环境结论一致，说明结果不依赖运行环境。

| 指标 | 实测值（v2 云端首次运行） |
| --- | --- |
| 库中条目 | **234**（v1 本地首轮 231） |
| 中文源条目 / 英文源条目 | 102 / 132（v1：102 / 129） |
| 官方一手源条目 | 36（v1：35） |
| 带发布时间（`published_at` 非空） | **234 / 234（100%）** |
| 无发布时间 | 0 |
| 跨源聚合（被并入事件簇） | 7（v1：7） |
| 有配图 | **64 / 234（27.4%）**，来自 **9 个源**（v1：49 / 231 = 21.2%，8 个源） |
| 近 24 小时 / 近 7 天 | 111 / 212 |
| 源英文展示名缺失 | **0**（曾出现 19 条，已修，见 DEVLOG 8.3） |
| 近 7 天源成功率 | 0.625 |

提供配图的 9 个源（条数）：Google AI Blog 15、Leiphone 13、The Verge AI 11、iFanr 11、
TMTPost 5、Simon Willison 3、MIT Tech Review AI 3、QbitAI 2、OpenAI News 1。

> 云端那次比本地多出配图，是因为境外出口下 `Google AI Blog` 抓取成功（15 条**全部**带图），
> 而本地直连时它超时失败 —— 这正是 1.3 的失败隔离场景。

**真实数据来源核对**（`tools/audit_sources.py` 输出）：16 个源的 HTTP 状态、条目数、
最新条目时间、日期格式与时间可信度均已实测记录在 `data/runs.json` 的 `sources[]` 中。

### 1.2 固定输入重复导入 —— 已验证 ✅（两条不变量）

**（a）同输入 + 同冻结时刻 → 产物逐字节相同**

```bash
python collect.py --input tests/fixtures/replay --now 2026-09-16T12:00:00Z --data-dir /tmp/a
python collect.py --input tests/fixtures/replay --now 2026-09-16T12:00:00Z --data-dir /tmp/b
cmp /tmp/a/news.json /tmp/b/news.json   # 同理 runs.json / update-status.json / meta.json
```

实测结果：**4 个产物全部逐字节一致**（`news.json`、`runs.json`、`update-status.json`、`meta.json`）。

实现要点：冻结时刻时墙钟耗时清零、`run_id` 由输入指纹派生（不再用随机 uuid）。

**（b）把同一批输入再合并一次 → 条目集合不变**

| 次数 | `run_id` | 新增 | 去重 | 存储 |
| --- | --- | --- | --- | --- |
| 第 1 次 | `20260916T120000Z-c1eeec` | 184 | 22 | **184** |
| 第 2 次 | `20260916T120000Z-c1eeec` | **0** | 206 | **184** |

幂等的三条不变量（由 `pipeline/store.py::merge` 保证，`test_store.py` 覆盖）：

1. `first_seen_at` 一经写入永不改变，重复导入只推进 `last_seen_at`；
2. 已存在条目不会被新副本替代，也不会产生第二条记录；
3. 易变字段（热度、摘要）的更新全部是**单调**操作（`max` / 更长者胜 / 补空），
   且合并前对入参做确定性排序 —— 因此结果与调用顺序无关。

### 1.3 单个来源失败 —— 已验证 ✅（离线确定性复现 + 生产记录）

把离线样本集复制一份，人为破坏其中一个源，与干净基线对照（同一批输入、同一冻结时刻）：

| 场景 | `overall` | 库中条目 | 受损源 `qbitai` 状态 |
| --- | --- | --- | --- |
| 基线（干净） | `success` | 184 | `ok`，保留 10 条 |
| 该源文件内容被损坏 | `success` | 176 | `parse_warning`，0 条（非法 XML 已容错但无条目） |
| 该源文件缺失 | `partial` | 176 | `failed`，0 条 |

**其它 15 个源在三种场景下完全一致 = True** —— 单个源出问题不影响任何其它源。

**退出码语义验证**：含失败源时退出码 **0**（`partial` 是正常结论）；
仅当「全部源都失败」才返回 **1**（实测 `--only google-ai` 且网络不可达时返回 1）。

**生产环境真实发生过的失败隔离**（`data/runs.json` 原始记录，共 16 轮）：

- 16 轮里 `overall` 分布：`success` 8、`no_new` 2、`partial` 6；
- **6 次 `partial` 的失败源每次都只有 1 个：`google-ai`**，
  `error="timeout：请求超时"`（中国大陆直连 `blog.google`，TLS 握手无响应）；
- 最早那次 `run_id=20260916T115115Z-31c36c`：该轮**其余 15 个源全部正常完成**；
- 云端首次运行（境外出口）时 `google-ai` 成功返回，该轮 `failed = 0`。

> ️ 注意：`google-ai` 的失败是**间歇性**的。因此「失败隔离」以**离线确定性实验**为主要证据，
> 生产记录只作为真实发生过的佐证。

### 1.4 任务配置及运行记录 —— 已验证 ✅（含云端首次执行）

**配置**（`.github/workflows/update.yml`，已写入仓库）：

| 项 | 值 |
| --- | --- |
| 触发 | `schedule: cron "0 22 * * *"`（UTC）= **北京时间每日 06:00** + `workflow_dispatch` 手动触发 |
| 并发 | `group: daily-update`、`cancel-in-progress: false`（两个实例同写 `data/` 会互相覆盖，故排队而不取消） |
| 权限 | `contents: write`、`pages: write`、`id-token: write` |
| 超时 | `timeout-minutes: 30`（避免单个源卡死拖到 6 小时默认上限） |
| 步骤 | 检出 → 装 Python 3.11 → 装依赖 → **采集** → **跑测试** → **构建并校验单文件** → 提交 `data/` → 上传 Pages 产物 → 配置 Pages → 部署 Pages |
| 时区换算 | `0 22 * * *` UTC = 北京次日 06:00，在中文读者起床前完成当天更新 |

**运行记录**：`data/runs.json` 保留**开发期间全部 16 次运行**，未做任何清理。
每次运行含 `run_id`、`started_at`/`finished_at`/`duration_ms`、`trigger`、`offline`、
逐源结果（`status`/`http_status`/`items_*`/`error`）与整轮计数。

见 1.8：workflow 已在 GitHub 上真实执行成功。

### 1.5 单元与集成测试 —— 已验证 ✅

```bash
python -m pytest tests/ -q
# → 205 passed in 5.60s
```

**205 项全部离线**（不访问网络），约 5.6 秒跑完。覆盖：日期解析（5 种实测格式）、去重、
幂等与原子写、事件聚合（含误聚回归用例）、打分与入选规则、配置健全性、配图提取、
离线逐字节可复现。CI 中每次 push 都会跑（`.github/workflows/check.yml`）——
**实测在 GitHub 上跑通，26 秒 success**。

### 1.6 前端真实交互 —— 已验证 ✅

```bash
python tools/verify_ui.py
# → 166 / 166 通过（三段：1500px 交互 / 1500px 回访 / 手机段 360px + 390px）
```

用无头 Chrome 渲染真实页面后**真的去点**：筛选标签、来源、语言徽标、主题按钮、「清除全部」，
断言**实际筛出来的集合**是否正确（而不是只看 DOM 里有没有生成元素）。
样例断言：`清除后回到全量 [261 / 261]`、`搜索生效且为子集`、
`所有缩略图盒子尺寸一致 [96x72]`、`没有标题渲染超过两行 [44px ≤ 47px]`、
`没有摘要渲染超过两行 [42px ≤ 47px]`、`主题可切换 [dark → light]`。

第二版为此新增三条护栏，把当轮发现的问题钉成回归：
`重点区右栏底部不留空洞（拉满到与特稿齐平）`、
`未读卡片正文对比度 ≥ 7:1（深色 · 浅色）`、
`初始卡片均为未读（无变淡）`。

另做了一次针对性的渲染核对（第一版 234 条构建）：**234 张卡片的时间文案各出现恰好 1 次**，
确认「时间显示两遍」缺陷已消除（曾有两个疑似命中，核实为标题/摘要正文里的「刚刚」等词，非缺陷）。

### 1.6.1 手机 / 窄屏版式 —— 已验证 ✅

第三版修的**主要是手机**：线上页在手机上右侧有一整截被裁掉，横向能拖动。定位过程见 `DEVLOG.md` 第 8.7 节，
这里只放结论与实测数字。以下 10 条断言全部在 **390px 视口**下判定（在 1500px 跑，窄屏缺陷根本不会出现）：

| 断言 | 修复前（390px） | 修复后（390px） |
| --- | --- | --- |
| 页面不横向滚动 | 文档宽 **549px** / 视口 390px ❌ | **390 / 390** ✅ |
| 没有元素越过右缘被裁 | **37** 个 ❌ | **0** 个 ✅ |
| 展开「数据面板」源表后仍不横向滚动 | 文档宽 **422px** ❌ | **390 / 390** ✅ |
| 粘性报头高度 ≤ 80px（不抢屏） | 53px ✅ | **55px** ✅ |
| 隐藏桌面快捷键提示 Ctrl K | 显示 ❌ | **display:none** ✅ |
| 搜索框放得下占位文字 | 需 81px / 可放 **50px** ❌ | 需 88px / 可放 **120px** ✅ |
| 「今日重点」标题区竖排，说明文字占满整行 | 横排，说明被挤到 **145px** 宽 ❌ | 竖排，**358px** ✅ |
| 可点控件触控高度 ≥ 32px | **386** 个不达标（最小 **11×10px**，是「语言」筛选）  | **0** 个 ✅ |
| 卡片缩略图仍在（没为了挤宽度砍配图） | 108×80 ✅ | **108×80** ✅ |
| 阅读账本没被窄屏藏掉（「只看未读」在且够大） | 23px ❌ | **35px** ✅ |

**「只改手机、电脑端一点没动」是证明出来的，不是看着像：** 所有改动都落在
`@media (max-width:1023px)` 与 `@media (max-width:640px)` 两段里（电脑端版式由
`@media (min-width:1024px)` 的 12 栏网格负责），并且在 **1024 / 1280 / 1500 三个宽度 × 深色浅色
共 6 张截图**上，改动前后 **SHA-256 逐字节完全一致**（`c89720c233347689` / `fe9c652c99bd84ea` /
`96255d289f75255d` / `a2bd4e5ab9ebb8dc` / `bb7af8c36ca27196` / `0567359a7153031d`）。

> 复现时注意两点，否则会看到「不一致」而误判：① 两个版本要在**同一分钟**渲染 —— 页面上有
> 「14 小时前」这类**按当前时间现算、小时级**的文案，跨小时会变一个字符（实测差异是元数据行里
> 9×9 像素的一处）；② 两次渲染要用**各自独立的浏览器 profile** —— 共用一个 profile 时前一次
> 写入的 localStorage 会被后一次读走。这个坑本轮真踩过，靠「独立 profile 复测 → 差异像素归零」
> 才排除掉是 CSS 引起的。

两个取数细节（都会影响别人复现，写下来）：

- **手机视口要多绕一步**：headless Chrome 的窗口宽度最小只有 512px（换 flags 绕不过，`--force-device-scale-factor`
  也不改 CSS 宽度，只改设备像素比）。所以用一个 390px 宽的 iframe 内嵌被测页面 —— 媒体查询才会按
  390px 求值；探针挂在 iframe 的 `load` 里**同步**测量，结果挂到全局，由外框页在它自己的 `load` 里读走
  （外框 load 必然晚于 iframe），避免与 `--virtual-time-budget` 抢时间导致「报告时有时无」。
- **折叠的 `<details>` 里的元素不算溢出**：Chrome 对折叠的 details 用 content-visibility 跳过渲染并裁剪，
  那些盒子的位置是假的。不排除它们会凭空多报 54 个「越界元素」（正是数据面板里的源表）。

### 1.6.2 手机端报头与字号（第四版）—— 已验证 ✅

第四版接着修手机：真机（iPhone，**393px** 视口）上站名和搜索框**叠在一起**，头条字大得像一堵墙。
定位过程见 `DEVLOG.md` 第 8.8 节。手机段从 1 档扩到 **2 档（360px / 390px）**，断言 10 → 15 条
（总数 99 → **119**）；360px 是安卓常见宽度，上一版只验 390px，正好漏掉了下面最后一行那个缺陷。

| 断言（393px 判定） | 第四版前 | 第四版 |
| --- | --- | --- |
| 报头里站名与搜索框不重叠 | 站名右缘 **135.2px** vs 搜索框左缘 **123.1px**，压 **12.1px** ❌ | 矩形**不相交**，垂直间隔 **11px**，报头分 **2 行** ✅ |
| 搜索框与两个图标按钮都点得到（命中测试） | 旧断言只查「有没有元素越过屏幕右缘」，**查不出这种压字** | 三个都命中自己 ✅ |
| 粘性报头高度 ≤ 80px（不抢屏） | 55px ✅ | **79px** ✅（站名行 32px + 搜索框行 36px） |
| 搜索框放得下占位文字 | 需 88px / 可放 **120px** | 需 88px / 可放 **241px**（360px 下 208px） ✅ |
| 头条标题 ≤ 20px 且行高 ≥ 1.35 | **22px / 1.28** ❌ | **19px / 行高比 1.45** ✅ |
| 「今日重点」不比站名大（层级不倒挂） | 区块标题 **24px** vs 站名 18px ❌ | **20px** vs 18px ✅ |
| 头条正文按真实类名拿到排版 | 继承 **13px 无衬线** ❌（CSS 写的 `.lead-hero .sum` 与 JS 渲染的 `lead-sum` 对不上，规则从未生效） | **14px 衬线 / 行高 1.8** ✅ |
| 页面不横向滚动 @ **360px** | 文档宽 **369px**（卡片网格轨道被 min-content 顶到 352px）  | **360 / 360** ✅ |
| 页面不横向滚动 @ 390px | 390 / 390 ✅ | **390 / 390** ✅ |

**「电脑端零变化」同样在第四版上重新证过**：改动全部落在窄屏两段，在**同一分钟、各自独立 profile**
下重渲 1024 / 1280 / 1500 × 深浅共 **6 张截图**，SHA-256 与第三版**逐字节相同**
（`be6ba5badff8ed45` / `9f6881398fe19bbb` / `4724b45bb056439d` / `b5e2a8cd19013539` /
`3f51d4622ef5e9c6` / `3e90553562425d1c`）。

> 这一轮补齐一个会让**整组比对空转**的坑：无头 Chrome 的 `prefers-color-scheme` **默认是深色**，
> 所以不能像上一轮那样「浅色不注入、深色才种 localStorage」，否则深浅两组渲染成同一个主题、
> 哈希当然相同 —— 看上去 6 组全绿，实际只验了 3 组。正确做法是**两档都显式种主题**，
> 并让脚本自证「同宽度下深浅两档的哈希必须不同」，这组比较才算数。

> 另外记一笔：`.lead-hero .sum` 与 `.lead-no` 这两条**从未生效**的规则，桌面端也一样没生效
> （头条正文同样是继承 13px 无衬线、元信息行也没拿到等宽小字）。第四版只按真实类名在**窄屏**重写了排版，
> 桌面端保持与第三版逐字节一致，没顺手改 —— 一处未被触碰的潜在缺陷，在这里如实记下。

### 1.6.3 窄屏「今日重点」横向轮播（第五版）—— 已验证 ✅

第五版把手机上**竖排堆叠**的重点卡片改成**横向 Banner 轮播**：一次主要看一条，自动轮播、
可左右滑动、底部有分页指示器与左右箭头、暂停/播放。定位与实现过程见 `DEVLOG.md` 第 8.9 节。
做法只有一条主线：窄屏把 `.lead-grid` 变成横向吸附轨道（**原生滚动 + `scroll-snap-type:x mandatory`**），
`.lead-side` / `.lead-mini` 两个纯包装盒用 `display:contents` 拆掉，让每张重点卡自己是一屏 ——
**DOM 结构一个节点都不动**，桌面那套 12 栏网格构造上碰不到。

`tools/verify_ui.py` 手机段（360px / 390px 两档）新增 **20 条**、电脑段新增 **2 条**，总数 **119 → 161**。

| 断言 | 关键实测（390px 视口，下同） |
| --- | --- |
| 重点区是横向吸附轨道，不是原来那摞竖排卡片 | `display:flex` / `snap=x mandatory` / `overflow-x:auto` ✅ |
| 轨道留得住手指滑动 | `touch-action:auto`（没被禁掉，原生滚动即手势） ✅ |
| 屏数 = 重点条目数 = 指示点数 | 5 / 5 / 5 ✅ |
| 一次主要展示一条：屏宽 = 轨道宽 − 32px | 326px vs 轨道 358px ✅ |
| 静止时露出下一屏的边（看得出左右还有内容） | 露出 **20px** ✅ |
| **每屏内容都填得住（占比 ≥75%）** | **90 / 96 / 96 / 96 / 96 %** —— 改前纯文字那屏只有 **48%**，视觉上像「图没加载出来」 ❌ |
| 每屏等高、内容没有被硬切 | 504 / 504 ×5；`scrollHeight` 502 ≤ 框高 504 ✅ |
| 滑动动画由浏览器原生平滑滚动提供 | 轨道 `scroll-behavior:smooth` ✅ |
| 减弱动效时改走一跳到位 | `smooth=false`（代码确实读了这条偏好，没写死） ✅ |
| 控制条可见：左右箭头 + 指示点 + 暂停 | `#hlPrev` / `#hlDots`×5 / `#hlNext` / `#hlToggle` ✅ |
| 箭头与暂停按钮触控尺寸 ≥32px | 38×38 / 38×38 / 45×32 ✅ |
| 每颗指示点都点得到 | 5 颗全部 ≥32px 且命中自己（`elementFromPoint` 命中测试） ✅ |
| 点「下一条」真的切到下一屏 | 0 → 1 ✅ |
| 点「上一条」首尾相接 | 1 → **4**（第 1 屏往回 = 第 5 屏） ✅ |
| 点第 3 颗指示点真的切到第 3 屏 | 4 → 2 ✅ |
| 自动轮播默认开着、按「暂停」真停、再按真继续 | `playing=true → false → true`，按钮文字「暂停 → 播放 → 暂停」 ✅ |
| **电脑端**仍是网格版式、不显示控制条 | `display:grid` / `snap=none`；`#hlBar` 为 `display:none` ✅ |

另外用一份**独立临时探针**（不复用 `verify_ui.py` 的断言，跑完即删）复核过同一批行为，
并多验两条 `verify_ui.py` 里放不下的时序断言：**暂停后 7 秒内容没有偷偷走**、
**19 秒后自动播放确实把当前屏推进了**（`target` 0 → 4、`scrollLeft` 1364、当前屏左缘离轨道左缘 **0px**，说明吸附对齐、不卡在两屏之间）。

**「电脑端零变化」在第五版上重新证过**：用 `HEAD` 里的模板 + 同一份数据重建改动前的页面，
与当前 `dist` 在 1024 / 1280 / 1500 × 深浅共 **6 张截图**逐字节比 SHA-256 —— **6 / 6 相同**
（`c9f66c6ef103293b` / `fad906b0d0778198` / `181387d08c6c3c0b` / `4ef377e0c76c875a` /
`b06a9f7479bff10b` / `d07a9276dc002504`）。比对时钉了两件事，否则会**误报「不同」**：

> · **两页共用同一个「现在」**（注入固定 `Date.now`）：相对时间按**每条情报自己的时间戳**在任意分钟边界翻档，
> 不钉时钟就会随机差一个 9×9px 的单字符（第一次跑就踩上，误报 light 1280 不同）；
> · **禁掉过渡/动画**：某个淡入在两次截图里差一点点，会让整块背景差 **1 个灰阶**（19.7% 的像素、
> 幅度全部 = 1/255），看着像「不同」，其实不是改动留下的痕迹 —— 禁掉后 6 组全部逐字节相同。

> 三条只在无头环境里才遇到的取证陷阱，记在这里（都影响「怎么判」，不影响「改得对不对」）：
> 「`display:contents` 只改布局归属、**不改选择器匹配树**」—— 拆盒后的 4 屏要用后代选择器
> `#highlights .hl` 才吃得着，写成子选择器 `.lead-grid>.hl` 会让它们被压成 **32px**（探针一眼看出）；
> 「无头虚拟时间会把平滑滚动**一次补完**」—— 逐帧的滑动动画取不到轨迹，只能拿「CSS 声明 smooth +
> JS 传 `behavior:smooth`」两条机械证据，外加「减弱动效档轨迹单跳」做对照；
> 「无头 Chrome 把 `prefers-reduced-motion` 报成 `reduce`」—— 不模拟成正常档，自动播放那条路会被自己的代码关掉，
> 于是「自动轮播能不能跑」永远验不到（第五版因此把「减弱动效」的语义定为**只去掉滑动动画、不静默停掉轮播**，
> 并补了一个真能用的暂停/播放按钮满足 WCAG 2.2.2）。

### 1.7 单文件交付物 —— 已验证 ✅

```bash
python build_site.py --check
# → 已生成 dist/index.html（678.9 KB / 695,224 字节）
# → 自包含校验通过：单文件无外部本地资源引用，数据已内联。
# → sha256(26062c13213df27e…)（第六版 / 422 条数据；第五版为 662,676 字节 / 014de82ed56d7d38…，第四版为 637,593 字节 / 5a5425fd97b8a0b1…）
```

校验内容包括：占位符已被替换、产物中确实含条目数据、**不存在指向本地文件的 `<script src>`/`<link href>`**、
HTTP 取数兜底代码仍在（`dist/` 整体作为静态站目录时使用）。
双击 `dist/index.html` 与通过 HTTP 访问均能正常渲染（已实测）。

### 1.8 云端首次运行与线上部署 —— 已验证 ✅

```bash
gh workflow run update.yml --repo oi1784105-spec/ai-intel-station
```

**结果：11 个步骤全部 `success`**（整轮约 40 秒）：

| 步骤 | 结论 |
| --- | --- |
| 检出仓库 / 安装 Python / 安装依赖 | success |
| **采集并更新数据** | success（境外出口下 16 源 `failed = 0`） |
| **运行测试** | success（205 项在 Ubuntu 上同样全绿） |
| **构建单文件站点并校验自包含** | success |
| **提交数据变更** | success（产生提交 `c2d396d chore(data): 每日自动更新 2026-09-16`） |
| 上传 Pages 产物 / 配置 Pages / **部署到 GitHub Pages** | success |

**线上站点实测**：

```bash
curl -I https://oi1784105-spec.github.io/ai-intel-station/
# → HTTP 200，391,559 字节，<title>每日 AI 情报站</title>，内联数据 total=234
```

线上体积与本地 `dist/index.html` **完全一致（391,559 字节）**，说明云端与本地构建产物同源同构。
（该次线上产物是**初版**；第二版上线后的线上复核见 1.9。）

### 1.9 第二版快照（本地抓取 2026-09-17）—— 已验证 ✅

```bash
python collect.py --trigger manual     # 本机直连（未设任何代理环境变量）
# → 16 个源：整轮 81.5 秒；库中 276 条（+11 新增 / 去重 200）；配图 76 条
# → overall = partial，唯一失败源 google-ai（timeout，65.2 秒）
#
# 随后云端 workflow（GitHub Actions，出口在境外）又采集一轮并部署：+23 → 299 条，
# 也就是当前线上那一版。该轮 data/update-status.json：ok 3 / no_new 13 / failed 0（overall = success）。
#
# 说明：276 条之前先把两份数据分支做了合并 —— 云端那次（265 条）与本地直连那次（266 条）
# 各有一两条互不重叠，按「数据只增不减」的管线本意合并后再跑一轮，得到 276 条。
```

| 指标 | 实测值（截至本次提交的数据目录快照：422 条 / 23 轮） | 对照（v2 云端首次运行） |
| --- | --- | --- |
| 库中条目 | **422**（中文 184 / 英文 238） | 234 |
| 中文源 / 英文源条目 | **187 / 235** | 102 / 132 |
| 官方一手源条目 | **43** | 36 |
| 带发布时间 | **422 / 422（100%）** | 234 / 234（100%） |
| 无发布时间 | 0 | 0 |
| 跨源聚合（`cluster_size > 1`） | **4** | 7 |
| 有配图 | **108 / 422（25.6%）**，来自 9 个源 | 64 / 234（27.4%），9 个源 |
| 近 24 小时 / 近 7 天 | **135 / 388** | 111 / 212 |
| 源英文展示名缺失 | 0 | 0 |
| 近 7 天运行成功率（管线口径） | **0.6957**（23 轮里 16 轮整体无失败源） | 0.625（当时 16 轮里 10 轮） |
| 源级成功率（源次口径） | **361 / 368 = 98.1%** | — |

配图分布（条数）：雷锋网 24、The Verge AI 23、钛媒体 22、Google AI Blog 16、爱范儿 14、
Simon Willison 3、MIT Tech Review AI 3、量子位 2、OpenAI News 1。

两个成功率的**口径不同，都在上面写清**：
`meta.json.success_rate_7d` 由管线按「近 7 天里 `overall ∈ {success, no_new}` 的**轮次**占比」计算
（见 `pipeline/report.py::success_rate_7d`）—— 只要有一个源失败，整轮就不计入；
源级成功率则按**源次**计算。这也解释了为什么前者看起来低：23 轮里的 7 次失败**全部**来自同一个源（该源 23 轮里 8 成功 / 7 失败 / 8 轮无新条目）。

> 这 422 条与构建产物 `dist/index.html`（678.9 KB / 695,224 字节 / `sha256:26062c13213df27e…`）是**同一批**数据：
> 页面由 `build_site.py` 把数据内联进来，页脚读数「全站共 422 条（其中 5 条为重点）」即此项的直接证据。
> 重建一次产物再核哈希，字节与哈希**完全一致**（构建可复现）。
>
> **线上复核（第五版部署后，2026-09-17）**：`https://oi1784105-spec.github.io/ai-intel-station/` 实测
> HTTP 200、**662,676 字节**、`sha256:014de82ed56d7d38` —— 与本地 `dist/index.html` **字节级完全一致**
> （部署 run `35243172713`，success）；页面内联读数 `total = 398`，窄屏规则
> （`@media (max-width:1023px)`、`.kbd{display:none}`）与窄屏轮播（`#hlBar` / `scroll-snap-type:x mandatory`）均在，
> 确认线上跑的是第五版。
> 历史值：第四版 637,593 字节 / `sha256:5a5425fd97b8a0b1`；第三版 620,486 字节 / `sha256:f5554500b8adbbe9`；
> 第二版 516,581 字节 / `sha256:1e936ea5c68d8184`。

---

### 1.10 按测试题逐条自检（第六版）—— 已验证 ✅

测试题 §06 写明「作品验收会检查：固定输入重复导入、单个来源不可用、缺少日期与长标题、
搜索无结果、随机抽查原文，以及按照 README 在干净环境运行」，并允许「用明确标注的测试数据
或本地测试方式演示」。交付前按这几条做了一轮自检，**顺手改掉三处不达标的地方**：

| 自检发现 | 处理 |
| --- | --- |
| 422 条里 **89 条**源站 feed 没给摘要，卡片只剩一个标题 —— 不满足 §03-02「每条资讯展示标题、**简短摘要或来源简介**、来源名称、原始发布时间及原文链接」 | 前端加**来源简介兜底**：按来源 id 给一句「这个源是什么」的说明（只写订阅配置里能核实的属性：语言 / 类型 / 是否官方 / 是否做相关性过滤），一律以 `来源简介：` 开头，与文章摘要 `摘要：来源 feed 原文` 显式区分 —— **它是我们对来源的说明，不是对文章内容的概括，不补造事实** |
| 发布时间缺失时卡片写的是「时间未知」，而条款原文是「标注**发布时间未知**」 | 文案改为「发布时间未知」，并在函数注释里写明「绝不拿 `first_seen_at`（采集时间）顶上去冒充发布时间」 |
| 仓库里没有任何密钥，但 `.gitignore` 也没挡 `.env` | 补 `.env` / `.env.*` / `*.env` / `secrets.json` 四条，防以后随手加 token 时被误提交 |

三处顺手钉进 `tools/verify_ui.py`（桌面 3 项 + 手机段 2 项，**断言总数 161 → 166**）：
「每张卡片都带摘要或来源简介」「来源简介兜底行带显式前缀」「数据里出现的每个来源都能查到来源简介」。

自检脚本（一次性、**跑完即删，未留在仓库里**）与结论：

| # | 测试题点名的场景 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | **按 README 在干净环境运行**（§06） | ✅ | 新建空 venv → `pip install -r requirements.txt -r requirements-dev.txt` → `pytest tests/ -q` = **205 passed**；再离线重放 `collect.py --input tests/fixtures/replay` → 退出码 0、产出 4 个 JSON、184 条；依赖版本与 requirements 一致（httpx 0.28.1 / feedparser 6.0.14 / pydantic 2.13.4 / python-dateutil 2.9.0.post0 / pytest 9.1.1） |
| 2 | **固定输入重复导入**（§06 / §03-04） | ✅ | 同一输入 + 同一冻结时刻跑两次 → `news.json` **逐字节相同**（231,505 字节）；把产物当历史再合并同一批输入 → 条目 **184 → 184**（第 3 次运行 `total_new=0`、`total_dup=206`）；184 条的 id / 归一 URL / 标题**三者皆无重复** |
| 3 | **单个来源不可用**（§06 / §03-07） | ✅ | 从离线样本里删掉 `verge-ai.xml` 重跑：该源 `status=failed`、`error=missing_fixture`（如实上报，不静默吞掉）；其余 15 个源全部跑完（`no_new` 9 个、`parse_warning` 6 个）；已有 **184 条一条没少**；`update-status.json` 的 `overall=partial`，界面状态灯转 warn。另复现出 `stale`：`venturebeat-ai`「最新条目 19 天前，超过 14 天阈值」→ **没有新内容 / 解析告警 / 疑似停更 / 抓取失败 四种状态可区分** |
| 4 | **缺少日期**（§06 / §03-05） | ✅ | 用**明确标注的本地测试数据**（在真实数据上追加 1 条标题带 `【验收演示·测试数据】ZZACCEPTDEMO`、`published_at=null` 的条目）：卡片照常出现，时间位写 **「发布时间未知」**，且该行**不出现**「前 / 分钟 / 小时 / 天」字样 —— 没有拿采集时间冒充。**口径**：发布时间未知的条目无法判定是否落在时间窗内，因此**不进「今天 / 近 7 天」，只在「全部」里出现**（写在这里，免得被当成丢数据） |
| 5 | **长标题**（§06 / §03-08） | ✅ | 线上最长标题 177 字符（默认可见列表内最长 67 字符）：卡片标题裁剪在 **2 行内**（49px ≤ 两行 49px）、不横向溢出（标题 `scrollWidth == clientWidth`、页面 758 ≤ 758）、**原文完整保留**（DOM 文本与内联数据逐字相同，只做视觉裁剪） |
| 6 | **无图片**（§06 / §03-08） | ✅ | 422 条里 314 条没有配图：全部落到源色占位块（本页实测 121 个占位、**破图 0 个**） |
| 7 | **搜索无结果**（§06 / §03-03） | ✅ | 输入不存在的关键词 → `#empty` 显示「**没有符合条件的条目。** 当前条件：中文 / 检索：…」+「清空全部条件」按钮；点击后列表恢复（394 张卡片）且搜索框清空 |
| 8 | **按来源筛选 / 今天 / 近 7 天**（§03-03） | ✅ | 点来源页签后列表只剩该来源（来源名去重后仅 1 个）；「今天 127 ≤ 近 7 天 361 ≤ 全部 399」单调成立；生效条件写在指示条里（`筛选中：今天 × 来源：… × 清空全部条件`），可一键清除 |
| 9 | **随机抽查原文**（§06） | ✅ | 固定抽样 5 条（leiphone / arXiv / Simon Willison / TechCrunch 等）逐个请求原文页：**HTTP 200 = 5/5**，页面标题与库里标题**对得上 5/5** |
| 10 | **时区与日期范围口径**（§03-03） | ✅ | 界面固定按北京时间（UTC+8）呈现（`new Date(ms + 8*HOUR)`，中国大陆不实行夏令时，无需时区库）；页头写「最后更新 …（北京时间）」；窗口类筛选用 `generated_at` 作基准，口径与管线一致 |
| 11 | **密钥不进前端 / 公开仓库**（§03-08） | ✅ | 扫描 78 个被跟踪文件 + `dist/index.html`：密钥指纹**命中 0**；本项目**零密钥**（全部公开 RSS / 公开 API）；`.gitignore` 已挡 4 类密钥文件 |
| 12 | **外部内容安全渲染**（§03-08） | ✅ | 本项目不用 `escapeHtml`：DOM 一律 `createElement + textContent` 组装（`el()` 助手）；全文件只有 1 处 `innerHTML`，是**静态 SVG 字面量**；`insertAdjacentHTML` / `outerHTML=` / `document.write` **命中 0** |

![缺发布时间的条目在 390px 下的样子（本地测试数据：整页只含那一条标注条目）](docs/screenshots/mobile-v6-missing-date-TESTDATA.jpg)

> 上图由**明确标注的测试数据**生成（单条 `【验收演示·测试数据】ZZACCEPTDEMO` 条目，`published_at=null`），
> 不是线上数据。线上 422 条发布时间齐全，所以「发布时间未知」这条分支日常不会出现 —— 它只在
> 验收方按 §06 拿缺日期数据来试的时候才显形。

**本节没有新立的「已验证」结论**：验收自检里仍然未验证 / 未完成的，与下面 §2 / §3 两张表完全一致
（连续多日准点触发、长期运行、并发排队等）。

> **数字口径**：上表「398 / 85 / 293 / 今天 127 ≤ 近 7 天 361 ≤ 全部 399」是**自检当轮**的数据快照
> （那时库中 398 条）。自检之后云端又自动采了一轮 → 库中 **422 条 / 23 轮**，于是把当时抓出的问题
> **按当前数据再核一遍**：无摘要 **89 / 422**、无配图 **314 / 422**、卡片缺描述 **0 张**、破图 **0 个**、
> 16 个来源的来源简介**全覆盖**，`pytest` **205 passed**、`tools/verify_ui.py` **166 / 166**。
> 也就是说：结论不依赖某一轮数据，换一批数据照样成立。

---

## 2. 未验证（Not Yet Verified）

| 项 | 为什么未验证 | 现有替代证据 |
| --- | --- | --- |
| **连续多日的准点触发** | 已观察到一次 `schedule` **自然触发**（GitHub run `35165566489`，UTC 2026-09-17T00:12，比配置时刻 UTC 22:00 晚约 2 小时，属 GitHub 调度排队）；「连续多日是否都准点」在交付期内无法观察 | 该次 `schedule` 运行 11 步全绿、Pages 部署成功；它与手动触发共用同一个 job、同一条命令（`--trigger cron`），离线重放已证明幂等与确定性 |
| **长期连续运行（>1 天）** | 无法在交付期内观察 | 幂等性已由离线实验证明（重复导入不产生重复条目），数据目录不会随时间膨胀 |
| **代理 / 境外出口切换下的稳定性** | `google-ai` 的成败取决于当轮解析到哪个地址（不是墙） | 失败隔离已由离线确定性实验覆盖；本地直连实测 23 轮（该源 8 成功 / 7 失败 / 8 轮无新条目，失败那次固定耗掉约 65 秒），原因已定位到地址层（见 README 已知限制第 1 条） |
| **大规模源下线时的观感** | 未构造该场景 | 无图源占多数时用源色占位块兜底（已实测 299 条构建 / 294 张列表卡片） |
| **CDN 防盗链下的外链图片** | 未穷举 | 前端对图片挂了 `onerror` 回退占位块；`loading=lazy` + `referrerPolicy=no-referrer` |
| **多源同时长时间挂死** | 只有 1 个源受影响，未构造 | `timeout-minutes: 30` 兜住整轮 |
| **并发排队行为（`concurrency`）** | 未同时触发两次 | 配置为 `cancel-in-progress: false`，语义明确；单次运行仅约 40 秒 |

---

## 3. 未完成（Not Done）

均为**有意不做**，不是遗漏。砍单依据见 README「取舍顺序（时间不够时先砍什么）」与
`DEVLOG.md` 第五节 —— AI 摘要正是按该顺序主动砍掉的第一项。

| 项 | 为什么不做 |
| --- | --- |
| **AI 摘要生成** | 砍单顺序第 1 位。它是唯一会引入外部 API 依赖与密钥的功能，与「零成本、零密钥、干净环境可跑」冲突 |
| **抓取文章页 `og:image`** | 需要 422 次额外请求，并引入图片体积、防盗链、被反爬三类问题。当前配图率 25.6% 是现状约束下的天花板 |
| **源级超时预算 / 熔断** | 当前只有一个源受影响（约 +65 秒/轮）。多源出现该情况时应引入 |
| **中文纯标题的事件聚合** | 见 README 已知限制第 3 条：跨源互证目前依赖拉丁专名。宁可漏聚，也不虚报「多来源报道」 |
| **实时搜索 / 分页** | 静态站形态的固有限制，数据全量内联（422 条约 679 KB） |
| **测试题 §11 的两个待确认决策** | 「摘要先关后加」与「今日重点」入选口径已按计划实现，未再另行确认 |

---

## 4. 复现步骤（面试官可逐条执行）

```bash
# 0) 准备
python -m venv .venv && . .venv/bin/activate      # Windows: .venv\Scripts\activate
pip install -r requirements.txt

# 1) 真实采集（需要外网；大陆直连时 google-ai 可能失败，属正常）
python collect.py --trigger manual

# 2) 幂等与确定性（全离线，不访问网络）
python collect.py --input tests/fixtures/replay --now 2026-09-16T12:00:00Z --data-dir /tmp/a
python collect.py --input tests/fixtures/replay --now 2026-09-16T12:00:00Z --data-dir /tmp/b
cmp /tmp/a/news.json /tmp/b/news.json && echo 逐字节一致
python collect.py --input tests/fixtures/replay --now 2026-09-16T12:00:00Z --data-dir /tmp/a
#   → 新增 0 条，存储条数不变

# 3) 测试
pip install -r requirements-dev.txt
python -m pytest tests/ -q                        # → 205 passed

# 4) 构建单文件交付物并校验自包含
python build_site.py --check                      # → dist/index.html，校验通过

# 5) 前端真实点击校验（需要 Chrome；无 Chrome 时该步跳过，不影响其它步骤）
python tools/verify_ui.py                         # → 166 / 166 通过（含 360px / 390px 手机段）
```

> 说明：第 5 步依赖本机 Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`）。
> 它是**可选的验收手段**，不属于运行时依赖。
>
> 线上站点：<https://oi1784105-spec.github.io/ai-intel-station/>

---

## 5. 原始证据索引

| 文件 / 路径 | 内容 | 提交 |
| --- | --- | --- |
| `data/runs.json` | **17 次运行的完整记录**（逐源状态、错误、计数、耗时） | ✅ 已提交 |
| `data/update-status.json` | 最近一轮的整轮结论与逐源健康度 | ✅ 已提交 |
| `data/news.json` | 299 条归一化条目（含 `published_at` / `first_seen_at` / 配图 / 聚合） | ✅ 已提交 |
| `data/meta.json` | 页面摘要：最近运行、条目总数、源数、近 7 天成功率、5 条「今日重点」 | ✅ 已提交 |
| `tests/fixtures/replay/` | 17 个真实样本 + `manifest.json`（含 sha256），离线复现的基础 | ✅ 已提交 |
| `tools/audit_sources.py` | 源站审计：HTTP 状态、条目数、最新条目时间、时间可信度 | ✅ 已提交 |
| `tools/capture_fixtures.py` | 重新抓取离线样本并更新清单 | ✅ 已提交 |
| `tools/verify_ui.py` | 无头 Chrome 真实点击校验（166 项，含 360px / 390px 手机段） | ✅ 已提交 |
| `dist/index.html` | 单文件交付物（数据已内联，695,224 字节） | ✅ 已提交 |
| `DEVLOG.md` | 实际耗时、返工与排查过程 | ✅ 已提交 |
| GitHub Actions 运行 `35103218567` | 云端首次运行的完整日志（11 步全绿） | ✅ 线上可查 |